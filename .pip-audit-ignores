PYSEC-2026-2447 diskcache 5.6.3 unsafe pickle deserialization: no fixed release exists (latest is 5.6.3); diskcache is a hard dependency of dspy and is used only as a local on-disk cache the agent user already owns -- an attacker who can write the cache dir already runs code as that user. Re-grade when diskcache ships a fix.
