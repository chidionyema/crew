# Recorded pip-audit dismissals (read by idp bin/estate-security-scan; format: "<ID> <reason>").
PYSEC-2026-3740 nltk 3.10.3 is the newest release on PyPI as of 2026-09-03 (measured: pypi.org/pypi/nltk/json info.version == 3.10.3), so no fixed version exists to move to; nltk arrives transitively through gpt-researcher in requirements-research.txt, which only the research worker installs (CI and serving paths never do, see the file's own header); revisit when nltk publishes a release above 3.10.3.
