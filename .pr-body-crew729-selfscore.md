crew#729: self scoring is banned forever. Founder, 2026-08-31, verbatim: "the research ledger should never score itself SELF SCORING IS BANNED FOREVER", "And evidence says research should score lowest", "NO FOUNDER CALIBRATE", "Because the estate reports to founder". The incident: the research grade page printed Outward ELITE from 30 rows the research lane wrote about itself, while the founder did the shelf-scanning. The fix: a tier above GAP now requires a delivery receipt authored outside the lane, and the receipt is a machine-checkable pointer (a merged pull request URL or repo@sha in used_in) — no person types a calibration anywhere. With zero receipts, which is the measured state today, the page floors at GAP and says why.

Optimised: 9 naive steps -> 4, 6 round trips -> 3; the grader edit, the page regeneration and both test files rode one scripted pass; the pre-fix red proof ran against origin/main's grader copied to scratch, no branch juggling.

Verify: `python3 -m pytest -q tests/test_incident_crew729_self_scoring_is_banned.py`
Verify: `python3 -m pytest -q tests/test_crew508_research_grade.py`

## Options considered
- Grade from the incident ledger (count founder-repeats against research): the signal exists but is authored by pipeline heuristics still under the crew#729 audit; building on it now grows the control plane the audit is subtracting. Rejected for this pass.
- Founder receipt as the lift (DoD v2.1 shape): rejected by the founder in the same breath — "NO FOUNDER CALIBRATE"; the estate reports to him, he does not feed its scorers.
- Chosen: receipts written by the consumer of the research, each carrying a used_in pointer to a merged PR or commit; the grader counts only rows with a verifiable pointer; zero receipts = GAP floor stated on the page.

## Architecture laws
- LAW 1 zero-gravity: science/DELIVERY-RECEIPTS.jsonl -> the only input that can lift a grade; science/research_grade.py grades() -> the one place the cap lives
- LAW 2 fractal: `python3 -m pytest -q tests/test_incident_crew729_self_scoring_is_banned.py`
- LAW 3 nervous system: tests/test_incident_crew729_self_scoring_is_banned.py (4: a perfect self-authored ledger never grades ELITE; one external receipt lifts the cap and only then; a receipt without a verifiable pointer does not count; the page states the ban and floors with no receipts); pre-fix red transcript below
- LAW 4 calibration: GAP floor in grades() -> the founder's stated evidence ("research should score lowest"); BLIND untouched -> a block that cannot see its source still says so first
Cost-delta-usd-month: 0
Drill: n/a — docs generator, no runtime surface
Breaker: n/a — no retry loop; a pure page generator
Control: tests/test_incident_crew729_self_scoring_is_banned.py
Tile: docs/science/RESEARCH-GRADE.md now opens with GAP and the ban, not ELITE

## Definition of done
1. Tracked item — crew#729
2. Code or config — grades() cap, delivered() reader, page section, regenerated RESEARCH-GRADE.md
3. Gate proved both ways — pre-fix: 4 failed on origin/main's grader (assert 'ELITE' != 'ELITE'); post-fix: 10 passed (transcript below)
4. Reference doc — the ban and the receipt shape are stated on the generated page itself, where every reader meets them
5. How-to and demo — a consuming lane appends one JSON line with what and used_in to science/DELIVERY-RECEIPTS.jsonl; the next hourly publish lifts the grade
6. Catalog entity — existing crew component; no new entity
7. Operational proof — regenerated page in this PR prints GAP/GAP from the live ledger
8. Scheduled re-grade — the existing hourly publish (crew#508) re-runs this grader unchanged
9. Standard row — no new tool; one existing generator edited
10. Evidence block — below

## Verification evidence
```
$ python3 -m pytest -q -o addopts= tests/test_incident_crew729_self_scoring_is_banned.py   # origin/main grader
4 failed in 0.07s   (assert 'ELITE' != 'ELITE' — the self-authored ledger graded ELITE)
$ python3 -m pytest -q -o addopts= tests/test_incident_crew729_self_scoring_is_banned.py tests/test_crew508_research_grade.py
10 passed in 0.14s
$ python3 science/research_grade.py
docs/science/RESEARCH-GRADE.md: 31 questions, 31 fed, 0 stale   (page now prints Outward GAP, Inward GAP)
```

Author-session: a7b41022

No-Issue: this advances crew#729 (the audit umbrella, which stays open for the founder CONFIRM) and closes nothing.

Accept when: `python3 science/research_grade.py --print` prints `| Outward | **GAP** |` and the page carries "SELF SCORING IS BANNED FOREVER" while science/DELIVERY-RECEIPTS.jsonl is absent, and `python3 -m pytest -q tests/test_incident_crew729_self_scoring_is_banned.py` exits 0.
