#!/usr/bin/env bash
# crew#609 CP2: the product readiness scorecard from what runs. Usage: bin/product-readiness [--check|--json]
set -euo pipefail
cd "$(dirname "$0")/.."
PY="${PYTHON:-python3}"; [ -x .venv/bin/python ] && PY=.venv/bin/python
exec "$PY" product/readiness.py "$@"
