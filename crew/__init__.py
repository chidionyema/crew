"""The crew: a conversation becomes a tracked, verified build.

The GitHub issue is the shared brain. This package is the only thing that
writes to it, so every agent reads and writes the same shapes.
"""

__version__ = "1.0.0"
