# Hazard register

GENERATED by `scripts/hazard-register` from `risk/REGISTER.jsonl` and the open P1 issues of chidionyema/crew, chidionyema/idp. Open hazards first, most likely first. Do not edit by hand; edit the register.

## The bar

- Hazards: **16 open**, 1 mitigated, closed or accepted, of 17
- Open P1 fires: **22**, 21 name the hazard they realised

| Id | Status | Likelihood | Hazard | What goes wrong | Mitigation | Residual | Realised by | Owner |
|---|---|---|---|---|---|---|---|---|
| R3 | open | certain -- it is a present state, not a future event | 17 of 23 repositories carry no licence file | A repository with no LICENSE grants a buyer nothing. Every module in those trees would need a licence decision before it could be sold, and any third-party code inside them needs auditing at the same time. | None today. The inventory measures it on every run, so the number cannot drift unnoticed. | Includes the repositories that hold the platform itself: .claude, .estate, crew, maestro, hermes-v2, survival-stack. | [crew#325](https://github.com/chidionyema/crew/issues/325) | platform |
| R4 | open | certain -- it is a present state | Work is recorded in 73 ledgers that do not join | No single query answers what is being worked on. The four largest are history.jsonl at 12,906 rows, state/prompt-ledger at 7,022, directives at 6,921 and one project's directive file at 5,392 -- and none of them joins to the others or to the GitHub issues. | The inventory names it as a silo finding on every run and counts the rows. The crew issue board is the intended single surface and already exists. | Consolidation has not started. Naming the silo is not closing it. | [crew#320](https://github.com/chidionyema/crew/issues/320)<br>[crew#283](https://github.com/chidionyema/crew/issues/283)<br>[crew#279](https://github.com/chidionyema/crew/issues/279)<br>[crew#71](https://github.com/chidionyema/crew/issues/71) | platform |
| R7 | open | certain that the ambiguity exists; unknown how many are real failures | 16 of 43 scheduled jobs report a non-clean exit and nobody has triaged which matter | 27 jobs report clean. The other 16 split as: 8 reporting SIGHUP, 3 SIGTERM, 3 not loaded, 1 SIGINT and 1 signal 78. For a long-running daemon a SIGTERM is a normal stop; for a periodic job the same status means it died. The two are indistinguishable in the current reading, so a genuinely dead job looks exactly like a healthy daemon. | The inventory decodes the wait(2) status correctly rather than reading it as an exit code, which is the trap that produced earlier false readings. recovery-posture passed today and asserts every job restarts, reschedules or is explicitly retired. | Per-job triage has not been done. Two of the three not-loaded jobs are correct by design: ai.hermes.gateway is deliberately kept out of launchctl, and com.adobe.ccxprocess is not ours. | [crew#340](https://github.com/chidionyema/crew/issues/340)<br>[idp#240](https://github.com/chidionyema/idp/issues/240) | platform |
| R9 | open | certain -- it is a present state | One database exists in two places with no stated authority | experience_graph.db is at ~/.estate/knowledge/maestro/ and at ~/.maestro/. Two copies of a knowledge store, and nothing says which is authoritative. | The inventory reports it as a duplicate finding on every run. | Not resolved. Naming which copy wins is a five-minute decision nobody has made. | not yet | platform |
| R10 | open | certain -- it is a present state | ~/dev/code is itself a git repository with no remote, and no inventory row | Ten project repositories sit inside a parent repository that has no origin. Anything committed at the parent level -- and it has commits -- exists on this laptop and nowhere else. The inventory does not list it, so the LAW 24 sweep and the offsite-backup drill both skip it and both stay green. | None yet. Found while putting ~/dev/code/idp into git on 2026-08-24. | The fix is one decision, not one command: either the parent stops being a repository, or it gets a remote and a row. Both are the founder's call because both change what is published. | [crew#32](https://github.com/chidionyema/crew/issues/32) | platform |
| R12 | open | certain the gap exists today; certain to fire the first time a swap carries real tool use | A provider swap is not a config change: three things do not port | The no-anthropic drill proves a model answers and something can edit a file. It does not exercise the three surfaces that actually break on a swap. First, tool-call schemas differ structurally: OpenAI takes `parameters` with JSON-string arguments, Anthropic takes `input_schema` with a parsed dict, Gemini takes protobuf-derived types, so every tool definition either errors or silently mis-parses its arguments. Second, Anthropic thinking blocks do not port at all -- Anthropic's own documentation says to strip them when switching models, and left in place they either 400 or are billed and ignored. Third, prompt caching is per-provider tuning, so a swap with the cache breakpoints left where they were reads as 'the new provider is slower and more expensive' when it is an untuned cache. | The abstraction exists and is not hand-rolled per provider: prospector/operator.py holds one Operator ABC with ten subclasses: eight in operator.py (Gemini, MiniMax, DeepSeek, OpenRouter, Ollama, OpenAICompatible, Mock, Fallback) plus ClaudeCliOperator and GeminiCliOperator in their own modules, built by a config-driven factory rather than a hardcoded list. Adopting MCP as the tool-definition surface removes the first of the three, because the schema is then defined once and translated at the boundary. Count is re-runnable, from prospector-main: grep -rhc '^class .*(Operator):' prospector/operator.py prospector/claude_cli.py prospector/gemini_cli.py \| awk '{s+=$1} END {print s" Operator subclasses"}' -> 10 Operator subclasses. | Nothing removes the second and third; they have to be handled at the boundary on every swap. And no test on this estate runs one tool schema through more than one provider, so the first failure would be found in production rather than in CI. Sources: platform.claude.com/docs/en/build-with-claude/thinking (strip thinking blocks when switching models), futuresearch.ai/blog/llm-provider-quirks (schema shapes). | not yet | platform |
| R15 | open | high | Spend has no cap that enforces itself | Daily spend runs to $866 against a $120 cap and nothing stops a session; one owner is 69% of it. The cap is a number on a page, not a circuit. | costsentinel (Dagster) reports; feed_meter.py prints the day's figure on every handoff; crew#26 tracks the cap. | Realised: crew#26. Reporting exists, refusal does not: no session is stopped when the cap is crossed. | [crew#26](https://github.com/chidionyema/crew/issues/26) | founder |
| R16 | open | high | Live configuration that no repository holds | A policy, a config file or a service definition exists only on a machine or in a console. It is edited by hand, drifts from what git says, and cannot be rebuilt. | LAW 24 (if it is load-bearing, it is in git); tofu for cloud state; config-syntax-guard on files that are tracked. | Realised: crew#311 (estate-operators policy outside tofu). Nothing enumerates live configuration and diffs it against git. | [crew#311](https://github.com/chidionyema/crew/issues/311) | founder |
| R17 | open | high | A discontinued estate still runs and costs | Hermes was discontinued by founder ruling and its services, schedules and checkouts keep running, consuming CPU on the one machine and confusing every inventory. | crew#13 tracks the retirement; the catalog marks archived entities. | Realised: crew#13, open since 2026-08-24. hermes-v2 processes were among the top CPU consumers on 2026-08-27. | [crew#13](https://github.com/chidionyema/crew/issues/13) | founder |
| R1 | open | medium | Every agent on this estate runs on one provider | Anthropic goes down, suspends the account or prices us out, and all six agent sessions stop at the same moment. There is no degraded mode: the estate does not slow down, it halts. | The no-anthropic drill strips every Anthropic credential and checks two layers -- a model answering at all, and something that can actually edit a file. It passed today, 0.0 days old against a 2-day policy. The laws are symlinked into ~/.codex, ~/.gemini and ~/.cursor so a session on another provider inherits the same rules. | 113 of 194 catalogued assets still carry an Anthropic-specific path, name or format. The drill proves an agent can work without Anthropic; it does not prove the estate's machinery can. | [crew#248](https://github.com/chidionyema/crew/issues/248)<br>[crew#116](https://github.com/chidionyema/crew/issues/116)<br>[crew#116](https://github.com/chidionyema/crew/issues/116) | platform |
| R11 | open | medium | Licence detection is implemented twice and can disagree with itself | idp/bin/catalog-gen and idp/bin/db-gen each walk a repository looking for LICENSE, LICENCE, LICENSE.md, LICENSE.txt or COPYING. Two copies of one rule in two languages. The moment one learns about a new filename the two portals report different licence counts for the same estate, and both look authoritative. | Both are currently identical and both report 17 of 23 unlicensed, which is the same number an independent read of the inventory gave. Two angles agree today. | The real fix is a licence_file column in ~/.estate/state/inventory.json so both renderers read one measured fact instead of each measuring it again. That is a change to the inventory generator, which is a different module. | not yet | platform |
| R14 | open | medium | A secret travels through a chat or a log | A credential is pasted into Telegram, a transcript or a log line, and from there into a 6.5 GB transcript store nothing scans. Rotation is manual and late; the buyer's diligence finds the value in a file. | credential-guard.py on every tool call; gitleaks in every repo's security-scan; secrets only through sops+age and the vault-seed workflow. | Realised once: crew#407 (2026-08-27). Chat channels and transcripts are not scanned after the fact; a paste that passes the guard stays. | [crew#407](https://github.com/chidionyema/crew/issues/407) | founder |
| R2 | open | low each, high that at least one fires | Five exits have never been drilled once | secret-rotation, windows-rebuild, github-gone, cloudflare-gone and stripe-gone have no recorded run. Each is a supplier or a machine whose removal has been planned for and never rehearsed. | The other eight drills pass and are fresh: rebuild, estate-bundle-restore, offsite-backup-restore, key-escrow-restore, telegram-delivery, db-integrity, recovery-posture and no-anthropic all ran today. The harness, the register and the verdict ledger all exist -- only these five bodies are missing. | stripe-gone is the most expensive of the five if it fires, because it is the only one that stops money arriving rather than work happening. | not yet | platform |
| R5 | open | low that the Mac dies; certain that the exposure is open right now | Uncommitted work exists only on this Mac | 17 of 23 trees are dirty. hermes holds 71 uncommitted files, ecommerce-frontend 28, prospector 17, .claude 13, crew 10. If the machine dies, committed work survives and this does not. | Committed work is genuinely safe: estate-bundle-restore and offsite-backup-restore both passed today, and both clone the bundle back rather than trusting the upload receipt. Every repository has a remote. | No backup covers an uncommitted working tree, and none should -- the fix is committing, not backing up scratch. | [crew#326](https://github.com/chidionyema/crew/issues/326)<br>[crew#319](https://github.com/chidionyema/crew/issues/319) | platform |
| R6 | open | low | The estate runs on one machine | One Mac holds the scheduled jobs, the ledgers, the guards and the working trees. It is the single point of failure for everything not already on a remote. | The rebuild drill passed today: it stands the estate up in a throwaway home from the remotes alone, so the recovery path is rehearsed rather than assumed. | windows-rebuild has never run, so the drill only proves recovery onto the same kind of machine. If the replacement is a Windows laptop the rehearsed path does not apply. | [crew#318](https://github.com/chidionyema/crew/issues/318)<br>[crew#313](https://github.com/chidionyema/crew/issues/313)<br>[crew#290](https://github.com/chidionyema/crew/issues/290)<br>[crew#85](https://github.com/chidionyema/crew/issues/85) | platform |
| R13 | open | low | One personal Google account is the login and the recovery route for every provider | The founder's Gmail is suspended, phished or lost, and GitHub, Oracle Cloud, Cloudflare, Stripe, Anthropic, OpenRouter and the Apple ID go with it. Recovery for each lands in the same mailbox, so nothing comes back. A buyer cannot take ownership of anything without the founder handing over one personal account. | idp/docs/reference/owner-accounts.yaml inventories the nine providers; bin/owner-account-gate counts the single-owner ones and bin/idp-ci proves it both ways. Security policy row A.5.2/A.5.30 names the target 0. | 9 of 9 providers single-owner on 2026-08-25. Closing needs a second owner identity per provider and recovery moved to a hardware key (crew#227 CP5, CP7). | [crew#345](https://github.com/chidionyema/crew/issues/345)<br>[crew#290](https://github.com/chidionyema/crew/issues/290) | founder |
| R8 | mitigated | certain -- it is a present state | There is no buyer-facing surface | Everything the estate knows is delivered by push to one phone or read by an agent. An outsider arriving today can open nothing and verify nothing without a session narrating it to them. | Built and serving as of 2026-08-24. Two renderers over one inventory: Backstage on node at :3100 and Datasette on python at :8001, published on the tailnet at :8443 and :10000. bin/idp-verify proves what each renderer serves matches the 194-entity inventory entity by entity, and the five diligence counts are the front page of the fallback. | Tailnet only. An outsider still cannot open it without bin/idp-publish on, which needs HTTPS certificates enabled in the Tailscale admin console -- a browser step, and the founder's decision. So the surface exists and is proved; handing it to a stranger is one switch away, not zero. The receipt here is a request to both renderers rather than a path to bin/idp-verify, because that script lives in ~/dev/code/idp and a receipt only this machine can run is not a receipt. idp-verify is still the deeper proof and is named in the mitigation; this row proves the surface answers. | not yet | platform |

## Fires with no hazard behind them

- [crew#114](https://github.com/chidionyema/crew/issues/114) Fully operational today — the day board, 2026-08-24
