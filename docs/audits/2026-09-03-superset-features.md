# Superset: every feature the ticket promised, graded (2026-09-03)

Founder word, 2026-09-03: "superset, we have it woring, just need the al the featiures that was ib ticket" and "which nay be done". The ticket is decision 0018, docs/demo/superset.md, docs/onboarding/superset.md, docs/runbooks/superset-dashboards.md, docs/how-to/boot-dagster-and-superset.md and the bodies of idp PRs 1263 and 1279. Every promise in those texts is a row below, with the probe that measured it (cluster reads with `KUBECONFIG=~/.kube/oke-estate kubectl`, run 2026-09-03 16:40Z to 20:30Z UTC). States are `MEASURED_OK`, `MEASURED_FAIL` or `UNKNOWN` (crew#656).

Totals: 42 features; 27 MEASURED_OK; 12 MEASURED_FAIL; 3 UNKNOWN.

Of the 12 failures: 8 are one cause, the boardroom seed Job (`superset-boardroom-seed`) has not yet run green, and its import road is owned by idp PR 1315 (`extra: {}` in the database yaml; the run before it died on `KeyError: 'extra'`, the one before on `KeyError: 'chartId'`). Four are repo-owned and are built in idp PR "fix(superset): the drill grades the Superset door..." (branch fix/superset-features). The Metabase volume is the one cluster change and waits on the founder's merge (LAW 11).

| # | feature | source line | state | probe | what was built (PR) |
|---|---------|-------------|-------|-------|---------------------|
| 1 | Superset chart 0.22.4 (app 6.1.0) replaces Metabase behind the same gateway | 0018 L21: "Apache Superset (Helm chart 0.22.4, app 6.1.0) replaces it behind the same gateway" | MEASURED_OK | `kubectl get helmrelease -n observability superset` -> READY True, "Helm upgrade succeeded ... chart superset@0.22.4" | already done (1263 lineage) |
| 2 | Remote-user middleware copies X-Auth-Request-Email into REMOTE_USER; account created on first sight | 0018 L22-25 | MEASURED_OK | `kubectl get secret superset-config -o jsonpath=... | grep -c 'RECAPTCHA_PUBLIC_KEY\|EstateGatewayIdentity\|AUTH_REMOTE_USER\|register_superset_auth_view'` -> 7; `psql ... select username,active from ab_user` -> chidionyema@gmail.com active, createAdmin is false so only the header could have made it | already done |
| 3 | No login page, no second provider, no console step | 0018 L25-26; onboarding L18-20; demo L7 | MEASURED_OK (founder); the hourly drill never graded this door | row 2 receipt; `curl -o /dev/null -w '%{http_code} %{redirect_url}' https://superset.mumchimp.com/login/` -> 302 to the IDCS authorize URL (the gateway holds the door; a 302 is not "up") | drill door `superset` added to OTHER_SURFACES in bin/idp-login-drill (this PR) |
| 4 | Founder enrolled as Admin automatically on first sign-in | onboarding L3, L8 | MEASURED_OK | `psql ... ab_user join ab_user_role join ab_role` -> chidionyema@gmail.com Admin | already done |
| 5 | platform/observability runs the chart plus its own Postgres | 0018 L34-35 | MEASURED_OK | `kubectl get sts,pods -n observability` -> statefulset superset-db 1/1, superset-db-0 Running 28h | already done |
| 6 | Every metabase manifest, its Terraform and its demo/onboarding pages deleted | 0018 L35-37 | MEASURED_OK | `git ls-tree -r --name-only origin/main | grep -i metabase` -> only metabase-recovery-dump.yaml (added later, on purpose), its test and explanation page; `grep -rci metabase platform/oci/` -> 0 | already done |
| 7 | Edge listener https-superset, hostname superset.<zone> | 0018 L38-39 | MEASURED_OK | `kubectl get gateway -n prospector prospector-edge -o jsonpath=...listeners` -> https-superset=superset.mumchimp.com; `kubectl get httproute -A` -> observability/superset ["superset.mumchimp.com"] | already done |
| 8 | Vault gains superset-db-password and superset-secret-key, Terraform-minted | 0018 L40-41 | MEASURED_OK | `oci vault secret list --compartment-id <estate>` -> superset-secret-key ACTIVE, superset-db-password ACTIVE; `kubectl get externalsecret -n observability` -> superset, superset-db SecretSynced True | already done |
| 9 | metabase-db-password leaves the vault | 0018 L41 | MEASURED_OK | same vault list -> metabase-db-password PENDING_DELETION | already done |
| 10 | PVC pgdata-metabase-db-0 deleted after the swap | 0018 L42-44; runbook L27-32 "The founder deletes it ... kubectl -n observability delete pvc" | MEASURED_FAIL | `kubectl get pvc -A | grep metabase` -> pgdata-metabase-db-0 Bound 50Gi 41h; the runbook hands the founder a terminal command (LAW 31) | `platform/observability/metabase-volume-retire.yaml`: Job + ServiceAccount + Role limited to that one claim (this PR, declared, unwired); wiring waits on the founder's merge (LAW 11) |
| 11 | Decision 0016 superseded; its branch deleted unmerged | 0018 L45-46 | UNKNOWN | `git ls-remote --heads origin | grep -iE 'metabase|google'` not completed in this session | none |
| 12 | Superset reachable at https://superset.<zone> behind the gateway login | demo L7; runbook L3 | MEASURED_OK | `curl` on /, /health, /login/, /superset/dashboard/boardroom/ -> 302 to the IDCS authorize URL every time; `kubectl get deploy -n observability superset` -> 2/2; blackbox Probe monitoring/founder-surfaces carries both URLs | already done |
| 13 | "Langfuse (ClickHouse)" connection seeded automatically | demo L8 | MEASURED_FAIL | `psql ... select database_name from dbs` -> empty; `kubectl get job superset-boardroom-seed` -> Failed 0/1 | PR 1315 (seed import road) |
| 14 | "Boardroom" dashboard seeded, six charts | demo L8; runbook L41-47; 1263 body | MEASURED_FAIL | `select dashboard_title from dashboards` -> empty; `select count(*) from slices` -> 0; pod log ends `DashboardImportError ... KeyError: 'extra'` | PR 1315 |
| 15 | Trace data lives in ClickHouse, database langfuse, tables traces and observations | demo L8-9; onboarding L10-12 | MEASURED_OK | `kubectl exec chi-signoz-clickhouse-cluster-0-0-0 -- clickhouse-client -q ...` -> traces 21094, observations 23056, generations 22878 | already done |
| 16 | Two datasets over traces and observations (model_calls, trace_activity) | 1263 body "two datasets over traces and observations" | MEASURED_FAIL | `select table_name from tables` -> empty | PR 1315 |
| 17 | SQL Lab offers the connection | demo L9; onboarding L13 | MEASURED_FAIL | no row in dbs yet; `expose_in_sqllab: True` is in the seed | PR 1315 |
| 18 | Drill by prompt, model, cost, latency | demo L3; onboarding L16 | MEASURED_FAIL | no dataset rows yet; the seed's model_calls carries model, prompt_name, total_cost, latency_ms | PR 1315 |
| 19 | Save charts, add to dashboards, share with the team | demo L10-11; onboarding L15 | UNKNOWN | built-in Superset behaviour; graded only through a signed-in session, and drills grade pages not clicks (LAW 53) | none |
| 20 | Every call through the router is queryable, no separate pipeline | demo L15 | MEASURED_OK | row 15: the counts are read from the Langfuse store itself; the seed reads that store directly | already done |
| 21 | Demo recording from demos/superset.tape | demo L19-24 | MEASURED_OK, text stale | `ls -la docs/demos/` -> superset.gif 578858 bytes; the page and tape said "five declared files", `ls platform/observability | grep superset` -> 6 | count-free wording (this PR) |
| 22 | Onboarding names an "events table" | onboarding L14 | MEASURED_FAIL (doc) | no such table or dataset anywhere in the seed (`grep -c events superset-boardroom-seed.yaml` -> 0) | names model_calls and trace_activity, read from the seed by a test (this PR) |
| 23 | Seed job "adds them from git on every estate sync" | onboarding L10 | MEASURED_FAIL (doc) | a Job runs once; Flux recreates it only when the manifest changes (force annotation) | wording fixed (this PR) |
| 24 | Founder surfaces probe watches the URL and /health | runbook L10-11 | MEASURED_OK | `kubectl get probe -n monitoring founder-surfaces -o jsonpath=...static` -> both superset URLs | already done |
| 25 | Flux owns the release; the observability row reconciles | runbook L12-13; 1279 body "the observability Flux row stayed red" | MEASURED_FAIL | `kubectl get kustomization -n flux-system observability` -> READY False "stalled resources: Job/observability/superset-boardroom-seed status: Failed"; chaos and science wait on it | PR 1315 |
| 26 | App DB password from the vault through the superset-db secret | runbook L14-16 | MEASURED_OK | `kubectl get externalsecret -n observability superset-db` -> SecretSynced True | already done |
| 27 | Two web pods on different nodes, PDB superset | runbook L17-18; superset.yaml L61-71 | MEASURED_OK | pods on 10.0.159.197 and 10.0.148.221, restarts 0; `kubectl get pdb superset` -> maxUnavailable 1 | already done |
| 28 | Sign-in trouble points at the route carrying login-forward-auth | runbook L23 "platform/observability/httproute.yaml" | MEASURED_FAIL (doc) | that file is Langfuse's route; Superset's is superset-httproute.yaml | pointer fixed and the drill named (this PR) |
| 29 | Re-running the seed overwrites in place (fixed uuids) | runbook L45-46; 1263 body | UNKNOWN | needs two green runs; none yet | none |
| 30 | Changing the seed needs no name bump (force annotation) | 1279 body | MEASURED_OK | `kubectl get job superset-boardroom-seed` age reset to 92s at 16:50Z with new pods after the 1279 merge; runbook on main names the annotation | already done |
| 31 | Owner is the first active Admin, no username in the manifest | 1279 body | MEASURED_OK (git) | `grep -n 'find_role("Admin")' superset-boardroom-seed.yaml`; runtime receipt waits on a green seed | already done |
| 32 | Log prints BUNDLE-IMPORTED then BOARDROOM-SEEDED | 1279 body | MEASURED_FAIL | pod log shows BUNDLE-RENDERED then the importer traceback | PR 1315 |
| 33 | bootstrapScript installs the postgres and ClickHouse drivers; pods stop restarting | how-to L21-29; 1263 body | MEASURED_OK | `kubectl exec superset-... -- python3 -c 'import clickhouse_connect, psycopg2'` -> both import (psycopg2 2.9.9); restarts 0 over 3h | already done |
| 34 | Portal label backstage.io/kubernetes-id founder-dashboards on Deployment and Service | superset.yaml L241-261 | MEASURED_OK | `kubectl get deploy,svc superset -o jsonpath=...labels` -> founder-dashboards on both | already done |
| 35 | Catalogue entity founder-dashboards "Dashboards (Superset)" with an Open link | backstage/founder/catalog-info.yaml L134-146 | MEASURED_OK (git) | `grep -n founder-dashboards backstage/founder/catalog-info.yaml` | already done |
| 36 | RECAPTCHA keys defined empty so /login/ never 500s | superset.yaml L218-224 | MEASURED_OK | row 2 grep count includes RECAPTCHA_PUBLIC_KEY in the served config | already done |
| 37 | Demo and onboarding pages exist and are in the nav (LAW 32) | demo, onboarding; mkdocs.yml L92, L170 | MEASURED_OK | `grep -n superset mkdocs.yml` | already done |
| 38 | The login drill grades the Superset door ("Drill: login-drill" in both PR bodies) | 1263 and 1279 bodies | MEASURED_FAIL | `grep -n OTHER_SURFACES -A8 bin/idp-login-drill` -> signoz, healthchecks, llm, screen; no superset | door added; guard test_the_login_drill_grades_the_superset_door (this PR) |
| 39 | Secrets ride mounts, never env; ClickHouse password never leaves the pod | 1263 body | MEASURED_OK | `pytest tests/test_superset_boardroom_seed.py` -> test_seed_reads_secrets_from_mounts_never_env passes | already done |
| 40 | Admission shape: batch bucket, cpu under 225m, registry in full, TTL | 1263 body | MEASURED_OK | `bin/idp-kyverno-render platform/observability` (with the retire Job wired) -> pass 524, fail 0 | already done; retire Job inherits the shape |
| 41 | Metabase recovery dump reads the old volume and prints its content | docs/explanation/metabase-recovery.md | MEASURED_OK | `kubectl logs metabase-recovery-dump-ssvrg` -> questions=39 dashboards=1; names are Metabase sample data ("Aerodynamic Copper Knife trend", "Checkout funnel"...) | already done; the finding is what lets the volume go |
| 42 | Web pods get the ClickHouse driver through the same bootstrapScript | 1263 body | MEASURED_OK | row 33 | already done |

## What this leaves

- The seed (rows 13, 14, 16, 17, 18, 25, 32): PR 1315 is the fix; Flux recreates the Job within the observability row's 10-minute interval after it merges. The receipt to look for: `kubectl logs -n observability job/superset-boardroom-seed` ending `BUNDLE-IMPORTED` then `BOARDROOM-SEEDED Boardroom charts: 6`, then `kubectl get kustomization -n flux-system observability` READY True.
- The volume (row 10): FOUNDER ACTION, merge the wiring change once he has read row 41; the runbook then records the date.
- Rows 11, 19, 29 stay UNKNOWN until a probe exists for them; none is a service claim.
