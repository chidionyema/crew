# Work map after the session loss, 2026-09-03

Founder record: `~/.claude/docs/founder/2026-09-03T1526Z-hi-we-lost-all-our-seesions-and-need-a7b6337a.md`.
Companion: `2026-09-03-session-rescue.md` (what survived and where), `2026-09-03-five-day-capability-audit.md` (what was built).

Every state below was measured today between 15:30Z and 15:46Z: GitHub pull requests and runs over the API, the
cluster's Flux Kustomizations over the laptop kubeconfig (reads only), and the surviving session transcripts.

## 0. The fire, and it is going out

Every `oke-check` run from 03:35Z to 12:37Z today failed (33711941272, 33713430188, 33722592727, 33748681743,
33756246171). Root: Kustomization `kyverno` sat on "revision is not up to date"; `edge` waits on it; `external-secrets`
and `scheduling` wait on `edge`; `secret-store` waits on `external-secrets`; 40 rows above that were waiting,
including `otto-golden`, `observability`, `llm`, `backstage`, `tailscale`, `verification`. The founder's "deploy both"
apply run (33756246171, 12:37Z) failed inside this cascade, so nothing it was meant to do landed.

Measured 15:45Z: kyverno Ready since 15:44:07Z on main@1add83d3, edge 15:44:35Z, external-secrets and scheduling
15:44:37Z, secret-store 15:45:08Z. The dependents clear on their own intervals; a watcher is confirming the drain.

Left: (a) confirm the drain; (b) re-run `oke-check mode=apply`, the founder's word "deploy both" (session 54539261,
12:39Z) was never executed; (c) root cause of the twelve-hour kyverno stall, LAW 6, after the cluster is green; this
is the same class PR 1257 addressed for dns and keda ("tiers never wait upward").

## 1. Boardroom reports (session a14fc078, 763 turns)

Landed: idp#1263 boardroom seed from live traces (merged 13:16Z), idp#1256 founder dashboards recovered from the old
server's volume (12:24Z), superset remote-user login (branch content on main, 0 unmerged commits).
Never proven: the seed Job `superset-boardroom-seed` is applied by the `observability` row, which was waiting in the
cascade all afternoon. The session's chain was waiting for the receipt `BOARDROOM-SEEDED ... charts: 6` when it died.
Left: (1) after the drain, read the Job log for that receipt; (2) second angle: dashboard rows in `superset-db`;
(3) founder's open question "not superset, metabase?", answered with the dump receipt (Metabase held vendor samples
only, decision 0018); his word switches it; (4) branch `fix/reports-publish` is one chore commit, close it;
snapshot `rescue/2026-09-03/wt-reports` holds 3 uncommitted lines, inspect and fold or drop.

## 2. Otto and otto-golden (sessions 54539261, a2aed3c9, 90c3f0b9, 4a84e2ea)

Landed: hermes-v2#62 Otto Platform v1 (merged 09-01), idp#1014 Otto's memory, voice and traces as graded rows,
idp#1254 both Ottos answer on kimi with router keys minted by code (12:28Z), idp#1251 every router lane can move to
the console with one workflow run, idp#1244 shared Redis memory for the router. Kustomizations `otto-golden`,
`otto-golden-secret` and `otto-gateway` (added today) exist on the cluster; all three were waiting on secret-store.
Left: (1) the apply that mints the router keys failed with the cascade; re-run it, then verify both Ottos answer on
kimi (the "Pending" of 54539261's last handoff); (2) kimi and deepseek vendor keys are 401-dead, Otto answers on
minimax until reseeded; the founder's Kimi key was proven dead by the probe (session a2aed3c9); (3) the founder
rejected "a page where humans paste keys" as the same friction with better paint and asked for a standard where keys
stop being things anyone handles; that session ended BLOCKED on his one-line definition of the standard; (4) orphan
branches pushed today: `feat/otto-staging` (10 unmerged commits: the Telegram token never touches a file or a
terminal), `otto-gateway-manifests` (3: pin the build that carries the door's module), `otto-registration-reconciler`
(2: drop the reconciler's standing CPU request), `deepseek-build-lane` (1); `otto-image-roll` has 0 unmerged commits,
delete it; (5) hermes-v2 itself still runs as a launchd job on the Mac with 14 uncommitted files (scripts, deploy/,
config.yaml) now on `rescue/2026-09-03/hermes-v2`; onboarding it onto the Flux road is audit step 3; (6) crew#819
"a paying customer can onboard themselves" and crew#820 customer-readiness review: ticketed, not started.

## 3. Lockdown, git is the only writer (session 2c88870e, 198 turns)

Landed: idp#1257 dns and keda stop waiting above their tier, liveness probes on four workloads (13:03Z).
Open: idp#1265 `feat/flux-only-writes`, control 1 of 3 (Kyverno refuses create, update and delete from any
`ocid1.user.*` identity except the estate-ci break-glass user); its operating-model gate was red at 14:19Z and the
session was grading the body locally when it died; the bot's GraphQL quota is exhausted at 15:40Z so the check
list cannot be read this minute.
Left: (1) 1265 green and merged; (2) control 2: the laptop key becomes a read-only identity bound to `view`, the
administrator key leaves the Mac (measured today: the laptop kubeconfig answers yes to "can I do anything, anywhere");
(3) control 3: OpenTofu import blocks for the 48 console-made cloud objects and a daily plan that goes red on drift;
(4) directive item 3, drop the 15 git router rows, waits on a vault-seed run carrying a `router-rows` entry.

## 4. CI purge, audit, land-and-onboard (session 54539261, 1101 turns)

Landed: idp#1261 the CI purge (13:04Z), idp#1248 R76 prose-pinning purge, the five-day audit (crew branch
`audit/five-day-capability`, Telegram message 22083).
Left, the audit's own §5 in order: (1) commit the on-disk waves, now safe as rescue branches: the portal redesign
(`rescue/2026-09-03/idp`, 67 files: DoorGrid, homeLayout, doorCopy and ~25 home, nav and theme files), hermes-v2's
scripts and config, `.wt-idp-ts-tag` (44 files, 3,895 lines on `fix/tailscale-operator-tag`, the largest unlanded
wave), and the smaller ones listed in the rescue record; (2) triage the 67 real branches, merge or delete same day;
(3) onboard hermes-v2 and prospector onto the Flux road; (4) the admission rule: a board item cannot close until a
probe shows the capability installed and answering. The founder's last, unanswered question in that session was
whether the audited capabilities are "actually operational, fully wired"; the answer is a measured surface list
from the estate-state document, which this session could not read (no MCP endpoint configured on this Mac).

## 5. Portal redesign (crew#612, crew#780; sessions a14fc078 early, 2d8b3bd0, 438aecb8)

Landed: idp#1077 crew#612 brand, Create, Map, icons, real routes (Cursor's work); Ops and Tools pages.
Left: the 67-file redesign wave was never committed (now `rescue/2026-09-03/idp`); `fix/portal-look-crew612`
(4 unmerged commits, pushed today) and `feat/portal-modern-home` (1: one-click nav and a fixed home, not a drag
board) are open branches; crew#780 says the Backstage UI work is delivered with no commit behind it. One portal PR
built from the snapshot plus crew612, then #780 closed honestly.

## 6. Older sessions (2026-08-29 to 09-01), last measured state each

| session | lane | last state |
|---|---|---|
| fbe5efbf | science | verifying the science page in a real browser; SHOWCASE rows all GAP, 0 of 11 predictions scored |
| d5ae1960 | acceleration plan | hermes-v2#46 closed, claude-guards#195 open, idp#564 review; "the estate router carries no Claude model" |
| 78caaa17 | GitHub App per lane | chaos fix green, landing on its own branch off main |
| 2d8b3bd0 | Cursor road | put back on the founder's word |
| a7b41022 | verification plane | landed as the signed verification layer (audit item 1) |
| 14ed6c8b | provider-agnostic model stack | paused by the founder |
| 80471694 | tailscale | "sitting in a PR you have not approved"; `tailscale` row was waiting on secret-store; 44-file wave on `rescue/2026-09-03/wt-idp-ts-tag` |
| 41fd24d8 | infra automation lane | idp#1100 step 3 merged; queue mode stranded on `infra-crew/step-1-foundation` |
| f3f21d6e | estate audit | Tailscale join waiting on a seed secret |
| ef0354ef | two verifiers | idp#1100 green and merged |
| 438aecb8 | UI architect pairing | stopped by the founder |

## 7. The order

1. Cluster green again and the apply re-run (section 0). Everything in 1 and 2 falls out of it.
2. Boardroom receipt and Otto on kimi, measured, not described.
3. idp#1265 merged; controls 2 and 3.
4. Land the rescue snapshots: portal wave, tailscale wave, then the rest; triage the branches.
5. hermes-v2 and prospector onto the Flux road.
Two things are the founder's: the keyless vendor-intake standard (section 2, item 3) and superset versus metabase.
