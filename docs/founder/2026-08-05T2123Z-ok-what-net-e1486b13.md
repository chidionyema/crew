---
captured: 2026-08-05T21:23:10+00:00
session: 74f739b1-6395-49b2-abb1-14f7432fe7f9
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2616
source: founder prompt, verbatim (founder-doc-capture.py)
---

ok what net 
Production is repriced and verified. Waiting on the POPDD gate for the record commit.

Shipped and live:

- PR #105 (https://github.com/chidionyema/prospector/pull/105) merged to main at 22:03:37Z, all four checks SUCCESS, mergeStateStatus: CLEAN. Local first: 1199 passed, 3 skipped (Python), Passed! - Failed: 0, Passed: 257 (Store.Tests).
- Deployed — deploy-api.yml run 31051253560, sha 02473ef5, both jobs success. The route is live by probe, not by the deploy's own tick: PATCH /internal/catalog/does-not-exist/price returned 404 before (route absent) and now returns 401 unauthenticated, 401 with a wrong key, 404 with the real key (authenticated, pack not found, no mutation).
- C1 executed against production, 22:07Z — 13 of 61 packs. 5 cuts (side_hustle/uk 4900→2900), 8 rises (7900 ×5, 9900, 14900, 19900), 48 held, 0 without a dossier. Each read back from /catalog. Re-running the planner now reports move: 0  hold: 61; the live distribution is {'£49.00': 48, '£79.00': 5, '£29.00': 5, '£149.00': 1, '£199.00': 1, '£99.00': 1}.

The L0 drain did exactly what it was built for: cuts took floor = new price with until = now; rises held floor = 4900 until 2026-08-07T00:07Z. Until then MinBillablePence != PricePence on the 8 risen packs is correct, not drift. Storefront confirmed serving the new numbers — /pack/fbd10d6bdfcd5e31 → £199.00, /pack/0cc434887c47cb9a → £29.00 (the price is in __NEXT_DATA__; a £ regex over the page body returns identical unrelated copy on every pack and reads as a failed update).

Two things I hit:

The preflight caught a real bug before any write — a stripe StripeObject raises AttributeError on .get, so price.get("livemode") blew up rather than defaulting. Fixed to attribute access.

PR #106 went CONFLICTING and GitHub scheduled no checks at all (an empty rollup, not a pending one). Cause: #105 landed as a squash, so the branch cut from the pre-merge local branch diverged from main. Closed it and rebuilt the same two files on a branch cut from origin/main — commit is in the POPDD gate now.

Not proven, and I want this on the record: no end-to-end purchase was made at a new price. The endpoint ran CanBillPriceAsync against prod Stripe on all 13 new price ids before committing (Program.cs:847-865, reached because /catalog only lists IsListed packs, Program.cs:245) — that proves billability, not fulfilment. The PackPriceHistory rows are written in the same transaction (Program.cs:886-899) but there's no read endpoint, so they were not read back.

Handoff written to checkpoints/LATEST.md (prepended above the Hermes entry, which was preserved).
