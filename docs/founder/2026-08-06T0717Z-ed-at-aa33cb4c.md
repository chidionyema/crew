---
captured: 2026-08-06T07:17:20+00:00
session: f7f25e75-236e-44b6-95cf-0e2b67b267fc
cwd: /Users/chidionyema/Documents/code/prospector
chars: 3033
source: founder prompt, verbatim (founder-doc-capture.py)
---

ed at:

- Candidate.audience (a property, so no dossier JSON on disk changes) is the single normaliser both boundaries read. Two private ones would let the index store SMB_Owner and the catalogue row smb_owner, splitting one persona into two cohorts in every join — with both suites still green.
- SQLite: dossiers.audience + idx_audience, empty string never NULL (matching market).
- Catalogue row: the five C# sites from the typed-columns memory, plus a nullable EF migration and the storefront type.

Proof. POPDD --staged PASS across all three lanes (python 1389, dotnet 279, web 751), chain valid, tsc clean. The load-bearing check is the negative control: commenting out the one apply-block line in Program.cs fails 3 of the 4 new dotnet tests — the 4th correctly still passes, since it asserts absence. A publish POST returns 200 with an unmodelled field silently ignored, so every assertion is a read-back on both endpoints.

Backfill applied to the live store (backup at prospector.db.pre-audience.bak): 1419 rows stamped, 180 skipped with no dossier file, 26 whose JSON records no persona. It copies a recorded value into the index beside it — it infers nothing and rewrote no JSON, unlike backfill_market.py.

The finding it immediately produced — a 6.4× spread in firm-pass rate across the 8 personas (smb_owner 8.3% … public_sector_worker 1.3%, n=118–252 each) against a generator that rotates the audience axis uniformly (generate.py:472). I did not act on it. Two caveats before anyone does: this is vetting yield, not conversion, and a persona the moat passes easily is not necessarily one that buys.

Three things I deliberately did not do, each a call I don't think is mine:
1. No render site displays the persona. Putting "written for: primary carers" on a card narrows buyers — a product/copy decision.
2. Not a discovery facet. facets.ADVANTAGE already has a member spelled "audience" meaning the opposite end of the transaction ("this buyer already has an audience"), so folding it in would put two meanings behind one word in the browse filter.
3. The 61 live packs stay null until republished, and republish is destructive. So per-persona conversion starts forward-only from here. If that's too slow, a narrow PATCH /internal/catalog/{id}/audience is the fix (the facets PATCH is the template) — your call.

Two process notes. My first commit reported exit 0 but never landed: | tail masked fatal: cannot lock ref 'HEAD' when a concurrent session moved HEAD during the ~9-minute gate. Verified the final commit by git log and confirmed all 13 files are mine — the other session had tests/scheduler/test_daemon_heartbeat.py staged in the shared index, which is why paths were named explicitly. Saved that race as a memory. Also, that session landed the category-rainbow and mobile-fold items from the previous handoff.

Your question about real conversion volume is still open and still governs S3–S5 sequencing — /internal/analytics/summary is key-gated and I have not used a credential.
 ned to address these
