---
captured: 2026-08-06T10:23:17+00:00
session: 8fa71ca0-7e3a-4040-bc47-6277c4edefd0
cwd: /Users/chidionyema/Documents/code/prospector
chars: 23205
source: founder prompt, verbatim (founder-doc-capture.py)
---

update 
    def mark_exhausted(self, name, dead_for_s, *, error=""):
        self.dead[name] = dead_for_s
        self.errors[name] = error

    def clear(self, name): self.cleared.append(name); self.dead.pop(name, None)

  Ran 1 shell command

All green, and all three guards mutation-tested — each fails when I revert the fix it protects:

1456 passed, 3 skipped in 93.92s

Now restoring live service. Inspecting state and clearing the stale marks in one pass:

  Ran 1 shell command

Moat probe: OK, exit 0, 12.8s — the brain was alive the whole time it was benched. Marks cleared. Now the daemon restart path:

  Ran 2 shell commands

New pid 79003 (11:15:16) postdates the newest source edit (11:13:58) — it's serving the fixed code. Now the docs, which still describe the old moat:

  Ran 1 shell command

⏺ Update(CLAUDE.md)
Added 1 line, removed 1 line

**Follow RUN.md:** every run (on-demand vet, scheduled batch, signal intake) executes the eight steps in RUN.md exactly. The procedure is the guarantee.

**Use web tools for grounding:** every check is grounded in real fetched pages. The retrieval chain is `[ddg, exa, claude_cli]` (`config.yaml retrieval.provider
`) — free DuckDuckGo first, Exa second, Claude Code's own web search as the always-available backstop. Gemini is gone: there is no `gemini` key anywhere in `con
fig.yaml`. Verdicts are ruled only by a trusted moat brain — `MOAT_PRIMARY = {claude_cli, claude, cursor_cli}` (`prospector/operator.py:875`). DeepSeek/MiniMax
are reserved for non-critical generation and triage ONLY; when one of them serves a verdict because the moat was exhausted, the ruling is stamped `provisional`,
 never publishes on PASS, and is auto re-vetted (`prospector/operator.py:878 is_provisional_provider`).
**Use web tools for grounding:** every check is grounded in real fetched pages. The retrieval chain is `[ddg, exa, claude_cli]` (`config.yaml retrieval.provider
`) — free DuckDuckGo first, Exa second, Claude Code's own web search as the always-available backstop. Gemini is gone: there is no `gemini` key anywhere in `con
fig.yaml`. Verdicts are ruled only by a trusted moat brain — `MOAT_PRIMARY = {claude_cli, claude}` (`prospector/operator.py:889`); cursor_cli was deleted 2026-0
8-06 (founder directive). DeepSeek/MiniMax are reserved for non-critical generation and triage ONLY and are no longer on the verdict chain at all: `config.yaml
operator: [claude_cli]`, so a blind moat now DEFERS instead of serving a `provisional` ruling. `is_provisional_provider` (`prospector/operator.py:892`) remains
the enforcement — anything outside MOAT_PRIMARY that ever rules is stamped `provisional`, never publishes on PASS, and is auto re-vetted.

**Write every run to store/:** capture input (signal or candidate), all verdicts + sources, the kill gate if applicable, cost, and timing. This log is the audit
 trail and the basis for learning.


⏺ Update(CLAUDE.md)
Added 2 lines, removed 1 line

- **config.py** — loads operator, model, retrieval, thresholds, we; no hardcoded values
- **models.py** — data classes for Candidate, Verdict, Claim, Dossier, Pack; the contracts
- **operator.py** — swappable brain (Claude, Cursor CLI, DeepSeek,lls to the active model. `MOAT_PRIMARY` (`operator.py:875`) is the t
rusted set that may rule a verdict; DeepSeek and MiniMax sit in tiered non-critical chains for generation, prescreen, and scoring. If one of them rules because
the moat is exhausted, the result is stamped `provisional` and re-
- **operator.py** — swappable brain (Claude CLI, Claude API, DeepSeek, MiniMax, Ollama, Mock); routes calls to the active model. `MOAT_PRIMARY` (`operator.py:88
9`) is the trusted set that may rule a verdict; DeepSeek and MiniMcal chains for generation, prescreen, and scoring. If one of them ev
er rules, the result is stamped `provisional` and re-vetted, never finalised. `_build_operator` raises an explicit `ValueError` for the removed `cursor_cli` so
a stale config or plist fails loudly at startup instead of silentl.
- **errors.py / health.py** — the failover classifier and the persisted dead-mark store. `classify_exhaustion` (`errors.py:120`) separates TRANSIENT backpressur
e (429/503/529, `overloaded_error`) from PERMANENT exhaustion (402mit); PERMANENT wins ties, and HTTP codes are matched on word bounda
ries so a request id or byte count cannot bench a live brain. Transient failures get `TRANSIENT_EXHAUSTION_S` = 60s (`health.py:54`), permanent ones the 1h defa
ult. `_claim_probe` (`health.py:130`) makes the dead mark half-opee, machine-wide, since the claim is persisted — is let through on a
geometric backoff to re-probe, so a brain that recovers in 90s is back in 90s rather than at the end of its window.
- **retrieval.py** — the grounding chain `[ddg, exa, claude_cli]`  per-provider circuit breakers); fixture support for offline test. `
GeminiGroundingProvider` still exists in the file but no config selects it.
- **prompts.py** — loads and renders the prompts (generate, prescrdversarial, score, content_gen, claim_check, price_comparables)
- **generate.py** — entry point for generation; divergent candidate creation from signals

⏺ Update(CLAUDE.md)
Added 2 lines, removed 2 lines
- **store.py** — local catalogue state; reading/writing dossiers and listings
- **publish.py** — publish stub; on PASS, write listing JSON + pri
- **bridge.py** — the money rail's entry point: mints the provider Price object and writes the catalogue row. One `PriceDecision` feeds both, so the minted pric
e and the catalogue price cannot drift (a drift charges the buyer ent fence).
- **run.py** — CLI entry point; orchestrates the eight-step procedure in RUN.md. Builds the tiered non-critical chain `_NONCRITICAL_ORDER = (claude_cli, minimax
)` (`run.py:177`) for generation/prescreen/score. Handles moat exh--resume`. Persists failed signals to `signals/pending/` for `genera
te --resume`.
- **run.py** — CLI entry point; orchestrates the eight-step procedtiered non-critical chain `_NONCRITICAL_ORDER = (claude_cli, minimax
)` (`run.py:194`) for generation/prescreen/score. Handles moat exhaustion with DEFER + `vet --resume`. Persists failed signals to `signals/pending/` for `genera
te --resume`.

## Key constraints


- **Golden-set regression gates all changes.** Part 13B acceptance tests block ship if a prompt change causes a regression on mixed-sector discrimination.
- **Creativity lives in generation; constraint lives in verificati generation time; all gates (pre-screen, verify, kill-filter) are do
wnstream.
- **Two loops never merge.** Sales metrics (demand) tune what to oding integrity, golden-set discrimination) veto what may ship. Deman
d never overrides truth.
- **Non-critical chains run behind their own breaker, and never ru, prescreen, and scoring run on `claude_cli → minimax` (`run.py:177`
), behind a completely independent health file and circuit breaker. If every tier fails, the chain raises ProviderExhaustedError — it never silently promotes it
self into ruling. The signal is saved for `generate --resume`. Fouput claude_cli at the head, so a MOAT_PRIMARY brain now serves non-c
ritical work: deepseek was measured at HTTP 402 Payment Required and cursor_cli at its usage limit, leaving every generation/prescreen/score call to minimax — w
hich is non-deterministic on structured routing calls even at tempes returned a different tier across 3 repeat runs). The absolute rul
e is unchanged and is about VERDICTS, not tiers: DeepSeek/MiniMax never rule as trusted-final, and `is_provisional_provider` (`operator.py:878`) enforces it.
- **Non-critical chains run behind their own breaker, and never ru, prescreen, and scoring run on `claude_cli → minimax` (`run.py:194`
), behind a completely independent health file and circuit breaker. If every tier fails, the chain raises ProviderExhaustedError — it never silently promotes it
self into ruling. The signal is saved for `generate --resume`. Fouput claude_cli at the head, so a MOAT_PRIMARY brain now serves non-c
ritical work: deepseek was measured at HTTP 402 Payment Required and cursor_cli at its usage limit, leaving every generation/prescreen/score call to minimax — w
hich is non-deterministic on structured routing calls even at tempes returned a different tier across 3 repeat runs). The absolute rul
e is unchanged and is about VERDICTS, not tiers: DeepSeek/MiniMax never rule as trusted-final, and `is_provisional_provider` (`operator.py:878`) enforces it.
- **A dead brain must leave a trace.** A fallback chain that works — the run succeeds, so nothing looks wrong, while the head of the c
hain is a guaranteed failure paid before every call. Permanence is classified by one shared, tested function (`errors.looks_exhausted`), which every metered ada
pter uses; only a `ProviderExhaustedError` reaches `_health.mark_eat classifier misses is retried forever. 402/"payment required" was
missing from it until 2026-08-06.
- **Moat exhaustion = DEFER, not crash.** When every MOAT_PRIMARY gnal pipeline continues (generation/prescreen/score run on the non-c
ritical chain), verification defers, and `vet --resume` picks up when the moat recovers.
- **Price is a rung, and evidence and action are separate decisionetrieves cited willingness-to-pay anchors on by default; letting the
m move a price is a second, explicitly-enabled switch. Retrieving evidence and acting on it must never be the same config flag — that is how a catalogue re-pric
es itself the day a feature merges.

⏺ Update(CLAUDE.md)
Added 1 line, removed 1 line
- **Golden-set regression gates all changes.** Part 13B acceptancempt change causes a regression on mixed-sector discrimination.
- **Creativity lives in generation; constraint lives in verification.** Nothing is killed at generation time; all gates (pre-screen, verify, kill-filter) are do
wnstream.
- **Two loops never merge.** Sales metrics (demand) tune what to offer; truth metrics (grounding integrity, golden-set discrimination) veto what may ship. Deman
d never overrides truth.
- **Non-critical chains run behind their own breaker, and never rule a verdict.** Generation, prescreen, and scoring run on `claude_cli → minimax` (`run.py:194`
), behind a completely independent health file and circuit breaker chain raises ProviderExhaustedError — it never silently promotes it
self into ruling. The signal is saved for `generate --resume`. Founder directive 2026-08-06 put claude_cli at the head, so a MOAT_PRIMARY brain now serves non-c
ritical work: deepseek was measured at HTTP 402 Payment Required a limit, leaving every generation/prescreen/score call to minimax — w
hich is non-deterministic on structured routing calls even at temperature 0 (4 of 6 candidates returned a different tier across 3 repeat runs). The absolute rul
e is unchanged and is about VERDICTS, not tiers: DeepSeek/MiniMax l, and `is_provisional_provider` (`operator.py:878`) enforces it.
- **Non-critical chains run behind their own breaker, and never rule a verdict.** Generation, prescreen, and scoring run on `claude_cli → minimax` (`run.py:194`
), behind a completely independent health file and circuit breaker chain raises ProviderExhaustedError — it never silently promotes it
self into ruling. The signal is saved for `generate --resume`. Founder directive 2026-08-06 put claude_cli at the head, so a MOAT_PRIMARY brain now serves non-c
ritical work: deepseek was measured at HTTP 402 Payment Required a limit, leaving every generation/prescreen/score call to minimax — w
hich is non-deterministic on structured routing calls even at temperature 0 (4 of 6 candidates returned a different tier across 3 repeat runs). The absolute rul
e is unchanged and is about VERDICTS, not tiers: DeepSeek/MiniMax l, and `is_provisional_provider` (`operator.py:892`) enforces it.
- **A dead brain must leave a trace.** A fallback chain that works hides its own degradation — the run succeeds, so nothing looks wrong, while the head of the c
hain is a guaranteed failure paid before every call. Permanence is tested function (`errors.looks_exhausted`), which every metered ada
pter uses; only a `ProviderExhaustedError` reaches `_health.mark_exhausted`, so a failure that classifier misses is retried forever. 402/"payment required" was
missing from it until 2026-08-06.
- **Moat exhaustion = DEFER, not crash.** When every MOAT_PRIMARY brain is exhausted, the signal pipeline continues (generation/prescreen/score run on the non-c
ritical chain), verification defers, and `vet --resume` picks up w
- **Price is a rung, and evidence and action are separate decisions.** `price_comparables` retrieves cited willingness-to-pay anchors on by default; letting the
m move a price is a second, explicitly-enabled switch. Retrieving must never be the same config flag — that is how a catalogue re-pric
es itself the day a feature merges.

⏺ Update(CLAUDE.md)
Added 2 lines, removed 1 line
- **Two loops never merge.** Sales metrics (demand) tune what to offer; truth metrics (grounding integrity, golden-set discrimination) veto what may ship. Deman
d never overrides truth.
- **Non-critical chains run behind their own breaker, and never rule a verdict.** Generation, prescreen, and scoring run on `claude_cli → minimax` (`run.py:194`
), behind a completely independent health file and circuit breaker chain raises ProviderExhaustedError — it never silently promotes it
self into ruling. The signal is saved for `generate --resume`. Founder directive 2026-08-06 put claude_cli at the head, so a MOAT_PRIMARY brain now serves non-c
ritical work: deepseek was measured at HTTP 402 Payment Required a limit, leaving every generation/prescreen/score call to minimax — w
hich is non-deterministic on structured routing calls even at temperature 0 (4 of 6 candidates returned a different tier across 3 repeat runs). The absolute rul
e is unchanged and is about VERDICTS, not tiers: DeepSeek/MiniMax l, and `is_provisional_provider` (`operator.py:892`) enforces it.
- **A dead brain must leave a trace.** A fallback chain that works hides its own degradation — the run succeeds, so nothing looks wrong, while the head of the c
hain is a guaranteed failure paid before every call. Permanence is tested function (`errors.looks_exhausted`), which every metered ada
pter uses; only a `ProviderExhaustedError` reaches `_health.mark_exhausted`, so a failure that classifier misses is retried forever. 402/"payment required" was
missing from it until 2026-08-06.
- **Moat exhaustion = DEFER, not crash.** When every MOAT_PRIMARY brain is exhausted, the signal pipeline continues (generation/prescreen/score run on the non-c
ritical chain), verification defers, and `vet --resume` picks up w
- **Moat exhaustion = DEFER, not crash.** When every MOAT_PRIMARY brain is exhausted, verification defers and `vet --resume` picks up when the moat recovers. A
DEFER is not a worse answer than a provisional ruling, it is a cheSS can never publish, so it costs a full verdict run now AND a full
re-vet later to reach the conclusion a DEFER reaches once.
- **The daemon must not mint work the moat cannot finish.** `_moat/run_scheduled.py:180`) is a generation preflight: when EVERY truste
d brain carries a live dead mark, the tick is skipped, logged as `moat_blind`, and counted unproductive so the escalating 5m/10m/20m retry applies instead of th
e 2h cadence. One live brain is enough to run — it is a floor, not reads the raw `dead_until`, never `is_dead`, so a bookkeeping check
 can never consume the half-open probe slot a real verdict call should get. Without it (2026-08-06) the daemon generated provisional passes at the same rate a t
wo-hour `vet --resume` drain retired them: 229 → 230 rows, net flag for the same subscription CLI.
- **Price is a rung, and evidence and action are separate decisions.** `price_comparables` retrieves cited willingness-to-pay anchors on by default; letting the
m move a price is a second, explicitly-enabled switch. Retrieving must never be the same config flag — that is how a catalogue re-pric
es itself the day a feature merges.

  Ran 1 shell command

The whole git add aborted on one stale pathspec — those two files are already staged as deletions, so they don't exist in the worktree. Nothing else got staged (the status
output is the tell, not the exit code):

  Committed 392ce4c

Committed as 392ce4c; the POPDD python lane ran the full suite green. Now he drain's exact invocation:

  Ran 1 shell command

No dead marks after 20 minutes of live traffic. But the tick log shows a 57200s interval, all with result=None — worth resolving before I trust it:

  Ran 1 shell command

"dry_run": true on every one of those, and their pids (77738, 78355, 79208emon — those are the Hermes cron probes, by design, not daemon ticks. Thedaemon itself (79003) is doing real work on the new code:

10:17:52  Candidate survived all gates. Scoring...
10:18:27  Artifact deliverable operator: claude_cli     ← trusted brain, n

Starting the drain on the 35 provisional passes:

  Ran 1 shell command

⏺ Write(~/.claude/projects/-Users-chidionyema-Documents-code-prospector/checsilience.md)
Wrote 74 lines to ../../../.claude/projects/-Users-chidionyema-Documents-code-prospector/checkpoints/2026-08-06-moat-resilience.md
# 2026-08-06 — Moat resilience / self-healing (COMMITTED, service r

## Task & goal
User: "need to address properly, this is production live service" → "we need to get rid of
cursor_cli" → "this whole process needs to be more resilient, cleve
Goal: stop the daemon manufacturing provisional backlog faster than a drain could clear it.

## Diagnosis (all receipts re-verified on disk this session)
- `claude_cli` was marked exhausted **9 times in 70 min** (08:25, 0
  09:07:11 x2, 09:08, 09:09, 09:36) at 3600s each — while `env -u ANTHROPIC_API_KEY claude -p
… +64 lines

  Ran 2 shell commands, recalled 1 memory, wrote 2 memories

Done and verified. Summary with receipts:

Root cause — the moat was never exhausted; a substring benched it.
claude_cli answered env -u ANTHROPIC_API_KEY claude -p "reply with exactly: OK" → OK, exit 0, 12.8s, while carrying a dead-until-10:36Z mark. It had been marked exhausted nine
times in 70 minutes at 3600s each, each failure taking ~3s. Three defects

┌─────┬─────────────────────────────────────────────┬────────────────────────────────────────────────────────────────────────────────────────────┐
│  #  │                   Defect                    │                                                    Runtime proof                                                     │
├─────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────┤
│ 1   │ errors.py matched 429/402 as bare           │ looks_exhausted('connection reset after 4291 bytes') → True; same for req_id=a429f0 timeout, Error: 4290 tokens,     │
│     │ substrings                                  │ billing address inva                                                                       │
├─────┼─────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 2   │ Every exhaustion got DEFAULT_EXHAUSTION_S = │ backpressure and a sguishable                                                              │
│     │  3600                                       │                                                                                                                      │
├─────┼─────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────┤
│ 3   │ A dead mark was never re-probed inside its  │ _raw skips a dead brain outright → recovery at 90s invisible until 3600s                                             │
│     │ window                                      │                                                                                            │
└─────┴─────────────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

Consequence wasn't a crash — it was ALERT.txt 09:23Z: 15/15 verdicts ruled by FALLBACK brain, each provisional and owing a re-vet, while the drain fought the same CLI: 229 →   230 rows, net flat.
                                                                                                                                                                                Shipped — 392ce4c, 18 files, +726/−474. POPDD gate PASS (python lane); sui green in fixed and random order. Each guard is mutation-tested — reverting the word-boundary regex, the preflight, or the mark_exhausted signature fails its own test:                                                                                    
- errors.py:120 transient (429/503/529) vs permanent (402/credit/usage limit), word-boundary codes, permanent wins ties                                                         - health.py:54 60s floor for backpressure; health.py:130 half-open probe —e-wide (claim persisted, so daemon and drain can't both probe), geometricbackoff, strikes, persisted last_error                                                                                                                                          - config.yaml operator: [claude_cli] — a blind moat now DEFERs; MOAT_PRIMA cursor_cli deleted, _build_operator raises loudly on it; vet_workers 5 → 3
- run_scheduled.py:180 _moat_blind_reason — generation preflight, moat_blind tick + escalating retry. It reads raw dead_until, never is_dead, so bookkeeping can't eat the      probe slot. I deliberately removed the exemption for injected generate_fn is the same shape as the bug you caught in 9b783f6.
                                                                                                                                                                                Live state: stale marks cleared, provider_health.json live dead marks NONEDaemon restarted onto the new code (pid 79003, 11:15:16 — postdates thenewest edit at 11:13:58) and its log reads Candidate survived all gates → Artifact deliverable operator: claude_cli: trusted brain, no fallback. Drain running on the 35        provisional passes (pid 80300), already producing grounded supported check
                                                                                                                                                                                One correction to my earlier read: the 5-minute result=null ticks are not carry "dry_run": true and foreign pids (77738, 78355, 79208…). They're theHermes cron probes, by design.                                                                                                                                                 
Handoff written to checkpoints/2026-08-06-moat-resilience.md (pointer appended to LATEST.md, not overwritten). Still open: PRs #99/#102 need closing as superseded (your call), and the ANTHROPIC_API_KEY rotation is founder-only.

Safe point — type /clear (state saved, nothing will be lost). The drain is keeps running through it.
