---
captured: 2026-08-06T12:23:43+00:00
session: e20a3052-089e-4246-91a0-0db71c7eb331
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2759
source: founder prompt, verbatim (founder-doc-capture.py)
---

need to ship 

  Ran 1 shell command

Shipped. Two commits on wip/split-v2, both gate-green, pushed.

┌─────────┬────────────────────────────────────────────────────────────────┐
│         │                                                                │
├─────────┼────────────────────────────────────────────────────────────────┤
│ 0f39f8d │ engine: clip_to_sentence replaces [:600] at three call sites   │
├─────────┼────────────────────────────────────────────────────────────────┤
│ 7d429fe │ web: one check vocabulary, card naming parity, 9 smaller items │
└─────────┴────────────────────────────────────────────────────────────────┘

Verified, not asserted: tsc exit 0 · vitest 54 files / 777 tests · eslint 0 errors · npm run build OK · pytest 1546 passed, 3 skipped. The one remaining eslint warning (pack/[id].tsx:150) is byte-identical at HEAD — checked with git show.

Two critique items I did not implement, with reasons rather than silence:

- Monogram covers → rejected. Reusing /og/pack/[id] as a card cover means ~63 renders on the homepage, text illegible at the ~3.75× downscale a grid card imposes, and the OG title duplicating the card title directly beneath it. The render-cost half is a hypothesis — I never benchmarked it.
- Duplicate logo alt text → refuted. Logo.tsx has no alt= anywhere. The header's <Link aria-label> overrides its contents for accessible-name computation, so the inner sr-only brand name is never announced twice.

Not merged, deliberately. wip/split-v2 is 54 commits ahead of main and carries the other session's in-flight drain work — including uncommitted edits to prospector/metrics_store.py as of a minute ago. Landing path when you want it: gh pr create → gh pr merge --squash --admin (main requires signed commits).

---
On coordination — the concrete version:

1. One lane per session. Engine/scheduler in one, storefront in the other, and never both in the same paths. Today that boundary held by luck: their commits were all prospector/scheduler/* and mine all store_platform/*, which is why nothing was clobbered.
2. Branch per session, not a shared wip/*. The real cost isn't index.lock, it's that neither session can ship without dragging the other's half-finished commits along — exactly why I stopped at the push.
3. Kill the drain session. Five commits in two days on the same ~200 lines (5f01aec, 23c4266, 0efe40e, 5cc325a, 5d350e4) is the tell that it's being patched iteratively rather than designed once. One session that reads the whole drain path and fixes it in a single pass would cost less than the next three patches.

Handoff: checkpoints/2026-08-06-storefront-critique-tranche-2-shipped.md, pointer appended to LATEST.md.

Safe point — type /clear (state saved, nothing will be lost).
