---
captured: 2026-08-06T13:14:07+00:00
session: 3c237764-9d2f-4f17-a2eb-19fd95997659
cwd: /Users/chidionyema/Documents/code/prospector
chars: 1705
source: founder prompt, verbatim (founder-doc-capture.py)
---

what is the point of the drain? what is it actually costing us _still_sets_the_cursor_concurrency_knob now walks tools/*.sh and deploy/*.plist; re-adding the export fails it. Control center is pid 9040, cursor-env-count: 0.

One genuine defect fixed along the way. retrieval.py:1330 marked exhausted grounding providers with a flat 3600s into the moat's health file. retrieval.provider includes claude_cli — the same key the verdict chain looks up — so a search hiccup benched the verdict brain for an hour. Same defect class as operator.py:~975; this call site was missed.

Two things I did not act on, because both are yours to call:

The drain failed on your allowance, not on a bug. You've hit your monthly spend limit. The classifier at errors.py:104 already rules that permanent — verified by running it. There were 202 deferred rows left and re-draining them now would spend straight back into the same cap.

The liability rail can't see the limit that fired. The tick row reads $3.9072 of $20.00 spent today (+$406.7640 subscription-equivalent, uncapped). The daily cap counts API dollars; the monthly subscription limit that actually took the moat down is the number labelled uncapped. That's a design decision, not a fix I should make unilaterally.

And a caution: another session is live in this tree. 23c4266 and 0efe40e aren't mine, and the other session's own checkpoint note says "3 concurrent sessions on one tree". My bare git commit happened to stage cleanly, but it was a risk — --only <paths> from here.

Handoff written to checkpoints/2026-08-06-cursor-removal-and-grounding-health.md, pointer appended to LATEST.md, two memories added.

Safe point — type /clear (state saved, nothing
