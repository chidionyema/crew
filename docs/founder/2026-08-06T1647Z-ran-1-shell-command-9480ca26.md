---
captured: 2026-08-06T16:47:24+00:00
session: f69e6536-e009-46e5-a6ae-1249c7cad419
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2764
source: founder prompt, verbatim (founder-doc-capture.py)
---

Ran 1 shell command

Deep audit done — whole estate, 84,469 transcripts, 2.53 GB, one deterministic pass. $3,123.31 / 26,127 requests / 5,082 sessions.

Three findings overturn current assumptions:

1. The session floor is the lever, not delegation. Median request-#1 prompt is 31,289 tokens — system + tool schemas + MCP schemas + CLAUDE.md + MEMORY.md — and the estate averages only 5.1 requests per session, so that floor is paid 5,082 times. Cache_write is the single largest driver (~45%), which is exactly what a repeatedly-cold 31K prefix looks like. Every 10K cut off the floor ≈ $508. Not yet broken down per contributor — that's step one, and it's fully under your control (claude-in-chrome alone advertises ~25 tools, Gmail ~18).

2. graphify's cost ceiling is ~1%, not the headline. Consecutive read-only recon runs across the entire estate: 125 runs, $33.40. The 77.3k-token Explore sweep you flagged is genuinely wasteful, but on this evidence the category is small. Caveat below matters here.

3. Batching compliance is 17.7%. 17,295 single-tool-call turns costing $1,563.77 — 50.1% of all spend — against 3,717 multi-call turns at $342. The rule has been in CLAUDE.md for months and prose isn't enforcing it. That's a hook, not a paragraph.

And the one that blocks your biggest known lever: opus→sonnet is worth $1,117.88 (35.8%) — but settings.json says "model": "sonnet" and this session ran claude-opus-5 on all 19 requests. The setting isn't taking effect. Diagnose before banking the 40%.

What limits all of the above, stated plainly: subagent spend is invisible in these totals. isSidechain is false on all 26,127 main-transcript requests while 47 separate subagent transcript files exist. So $3,123 is a floor and the graphify/recon number is understated. This also corrects a MEMORY.md line claiming isSidechain is never emitted — it is; subagent turns just live elsewhere.

I also caught and fixed a methodology error mid-audit rather than shipping it: deduping records by message.id and keeping the first discards tool blocks, which made the estate look like it had 6 multi-call turns ever. Real answer: 3,717. Both the batching and recon numbers above are post-fix.

On your quality harness — repo-agnostic, $ per solved task not per request. Its feasibility rests on one unchecked thing: whether a headless claude -p run can be config-isolated and self-metered. That's the first thing to test; if it can't, the design changes entirely.

Checkpoint written with the ranked levers, the method trap, and exact next steps. I'm at ~132K resident against your 140K hard stop, and the next steps (parse 47 subagent files, decompose the floor, build the harness) all start clean.

Safe point — type /clear (state saved, nothing will be lost).
