---
captured: 2026-08-07T22:10:39+00:00
session: a685d699-114b-422a-8570-27d58e0ef15d
cwd: /Users/chidionyema/Documents/code/prospector
chars: 3771
source: founder prompt, verbatim (founder-doc-capture.py)
---

```
store_platform/src/Store.Web/src/lib/faqContent.ts
store_platform/src/Store.Web/src/pages/about.tsx
store_platform/src/Store.Web/src/pages/how-it-works.tsx
store_platform/src/Store.Web/src/pages/index.tsx
store_platform/src/Store.Web/src/pages/pack/[id].tsx
```

Everything else auto-merges, **including all four renames** — `HeroEvidenceStrip.tsx` auto-merged
clean, so git tracked the renames correctly and they are not part of the problem.

Do this first, on a fresh session with low resident context (conflict resolution at 141K is how you
get a bad merge):

1. `git worktree add --detach ../merge-130 fix/kill-log-smoke-selectors && ./scripts/setup_worktree.sh ../merge-130`
   (the script fixes four traps; `node_modules` must be `cp -Rc`, never symlinked).
2. `git merge origin/main`, resolve the 5 files. My side owns the §5.2 vocabulary ("evidence
   record") and the renamed component identifiers; main's side owns whatever landed in those 8
   commits. **Never `git add -A`** in a worktree — `store/` and `storage/` are tracked runtime state
   pytest writes to.
3. Verify: `npx tsc --noEmit`, `npx vitest run`, `npm run build` (capture its own exit status
   BEFORE any pipe — `| tail` reports tail's status), `python3 scripts/site_spec_probe.py`.
4. Push, then `gh pr checks 130` — and remember empty output means HEAD was never tested.

## After the merge — §6.7 intent search (P1, product)

Merge the "What skills do you bring?" picker at
`store_platform/src/Store.Web/src/components/discovery/FacetBar.tsx:52` into a single full-width
intent input.


  Ran 1 shell command

Committed, pushed, PR open — but not merged, and I won't tell you it's shipped when it isn't.

/clear                        Start a new session with empty context; previous session stays on disk (resumable with /resume)
/code-review                  Review the current diff for correctness bugs and reuse/simplification/efficiency cleanups at the given effort level (low/medium: fewer,
                              high-confidence findings; high→max: broader coverage, may include uncertain findings; ultra: deep multi-agent review in the cloud). Pass --com…
/simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for
                              bugs; use /code-review for that.
/simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for                               bugs; use /code-review for that.
/simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for                               bugs; use /code-review for that.
/simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for                               bugs; use /code-review for that.
/simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for                               bugs; use /code-review for that./simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for
                              bugs; use /code-review for that./simplify                     Review the changed code for reuse, simplification, efficiency, and altitude cleanups, then apply the fixes. Quality only — it does not hunt for
 need everthig shipped
