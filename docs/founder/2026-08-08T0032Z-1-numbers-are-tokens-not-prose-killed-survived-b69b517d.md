---
captured: 2026-08-08T00:32:14+00:00
session: 3be0f089-8bd0-4ab9-9d75-c8f34f180ec6
cwd: /Users/chidionyema/Documents/code/prospector
chars: 3878
source: founder prompt, verbatim (founder-doc-capture.py)
---

1. **Numbers are tokens, not prose.** `{KILLED}`, `{SURVIVED}`, `{PACKS}`, `{RATE}%` must be injected from one source of truth at build time. Never hand-typed. This fixes the four-different-kill-counts bug structurally, not editorially.
  2. **One primary CTA per screen.** Supporting links are visually subordinate.
  3. **Pack titles use one grammar:** plain verb-phrase job statements. No invented brand names on cards ("Kerb Cut", "RateRebase" → move brand names inside the pack).
  4. **No raw confidence scores on marketing surfaces.** Verdicts only: *Survived* / *Pushed back* / *Killed*. Scores live behind an expand.
  5. **Decision chips (B2C, Part automated, Full-time, etc.) appear on catalogue cards**, not just pack pages.
  6. Truncation always ends at a clause boundary. Never mid-word, never "the…"

  ---

  ## 1. HOMEPAGE

  ### Hero

  > **Business ideas with the research already done.**
  >
  > The buyer, the price, the margins, the plan. Every claim links to its source.
  >
  > **[Browse {PACKS} packs]**  ·  Read a full pack free — no card, no email

  *Notes: Your current hero is already the best copy on the site — kept almost verbatim. "From £29 · 56 packs" eyebrow line stays, tokenised. Sample link demoted to text link;
  browsing is the primary action.*

  ---

  ### The proof strip (replaces "The filter log" widget position — move dire

  > **{TOTAL} ideas researched. {SURVIVED} survived. That's {RATE}%.**
  >
  > The other {KILLED} are published, each with the evidence that killed it.

  *Notes: Your survival rate is the strongest stat you own. It currently hid Works. Put it where every visitor sees it in the first scroll. Theanimated kill ticker (✕ ChargeBreak — killed by incumbency) can live here; it's good theatre.*

  ---

  ### Newest on the shelf

  *(No copy change needed to the section title. Card copy follows the new grammar — see Card Template below.)*

  ---

  ### Catalogue intro

  **Current (67 words, says one thing three times):**
  > Every pack contains the same 8 documents. The price follows the size of ch found, never the length of the download. Same 8 documents in everypack. Why prices differ

  **Rewrite (22 words):**
  > Every
  ──── (492 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   The survivors are the packs. Right now: {KILLED} killed, {SURVIVED} survi

  ### The checks — reuse the six plain-English lines from §2. Don't restate nt" mechanic; the hero just said it.

  ### The kill log paragraph

  **Rewrite:**
  > Most ideas die. Every kill is public, with the argument that made it. The log is the receipt behind the catalogue; the catalogue is what's left.

  *Cut "The voice is source-or-die. Sourced, not sold. Refutational, not promotional." — that's a style guide talking to itself, not a shop talking to a customer.*

  ### Founder story — the homepage note belongs here in full, plus a paragraph if wanted. It's currently split oddly between the two pages; About is its home, homepage carries
  the short version.

  ### Closing

  > **See the work first.**
  > One full report, unredacted. Every check, every verdict, every source.
  > **[Read the free report]**

  ---

  ## 9. SHARED COMPONENTS

  ### Footer tagline (currently three variants exist: "survived the filter"
  Pick one:
  > Business ideas that survived the filter. Fully sourced, ready to build.

  ### Footer kill counter — tokenised, same source as everything else.

  ### Disclaimer (keep legal intact, tighten the lead):
  > Mumchimp packs are research, sold for information — not financial, legal or investment advice. We don't guarantee any business outcome. Payments by Stripe.

  ### Email capture — one canonical version (§1), reused everywhere. Never two forms on one page.

  --- review
