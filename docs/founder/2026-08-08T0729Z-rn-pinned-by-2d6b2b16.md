---
captured: 2026-08-08T07:29:31+00:00
session: 132b6a28-40db-422e-8f07-ddb68514dfad
cwd: /Users/chidionyema/Documents/code/prospector
chars: 4685
source: founder prompt, verbatim (founder-doc-capture.py)
---

rn; pinned by
  `test_g8_still_fires_when_the_old_refine_pass_is_switched_off` + converse.
- **New `coverage_sampler` keys took down collection** — rejected by the allow-list at
  `prospector/config.py:349`. Missed by every targeted test because they all build a fixture
  config and never load the real `config.yaml`. Now pinned by
  `test_the_shipped_config_yaml_actually_accepts_the_two_new_keys`.
- **Fixture mode was not offline** despite claiming "zero network, zero spend" — G2 fetches
  through the RETRIEVAL chain, not the operator, so `MockOperator` does not stop it (it wrote
  live briefs into `store/incumbent_cache.json`). First fix attempt failed too:
  `landscape.py:92` reads `icfg.get("providers") or _DEFAULT_PROVIDERS`, so `providers: []` is
  falsy and silently restores the defaults. Now disabled via `enabled: False`, verified by the
  absence of the cache file.
- **Corrected cost figure:** G8 is **+2 operator calls per wave**, not +1. The +1 assumed it
  replaces a refine call that `refinement_enabled: false` means is not being paid.

## The refine-prompt correction (the only default-path behaviour change)

`prompts/refine.md` said verbatim "Drop the weak/obvious ones." while the code has guaranteed
since 2026-07-02 that nothing may be dropped. The two resolve as: an idea the model omits is
passed through UNREFINED, so refinement was **anti-targeted** — the weakest ideas were exactly
the ones getting no improvement. Both `refine.md` and `refine_system.md` now critique and repair.

## Files touched (all committed)

new: `prospector/critique.py`, `prompts/critique{,_system}.md`, `prompts/revise{,_system}.md`,
`tests/unit/test_critique_revise.py`, `tests/unit/test_coverage_illumination.py`,
`tools/experiments/g_generation_ab.py`
modified: `config.yaml`, `docs/GENERATION_QUALITY_PROGRAM.md`, `prompts/refine{,_system}.md`,
`prospector/config.py`, `prospector/coverage.py`, `prospector/generate.py`,
`tests/invariants/test_house_voice.py`

## Standing constraints

Never `git add -A` (`store/`, `storage/` are tracked runtime state pytest writes to); always
`git commit --only <explicit paths>`, untracked files need an explicit `git add` first.
POPDD pre-commit runs ruff + the full ~2460-test suite — allow >=600s. Do not touch E1-owned
files: `prospector/claude_cli.py`, `tests/unit/test_e1_abort_on_outage.py`,
`tools/experiments/e1_hybrid_query_arms.py`. `cmd | tail` reports tail's exit status.

Shipped. origin/main is now 74cd36a, the merge of PR #135. CI green on all four checks, POPDD gate green at 2461 tests.

The proof run failed, and its numbers must not be used. The live A/B hit tay through:
                                                                                                                                                                            "Claude CLI skipped: usage wall is live"
"Generation batch 2.6 failed: claude cli not called: usage limit reached                                                                                                     — claude usage wall: capacity returns 2026-08-08 04:35:38 (15.0 min)"
                                                                                                                                                                            Several cells came back distinct_k=0/0 because generation returned nothingse as real observations and printed COMPLETE, which is how shipped -6.00and g8_critique_revise -1.50 appeared. Those are artifacts of an outage, not measurements — I built into this harness the exact failure its own docstring cites             (an-outage-is-the-end-of-the-measurement-not-a-datum). It needs fixing befcell must end the measurement the way a ProviderExhaustedError does.
                                                                                                                                                                            There is a second reason the run couldn't have ruled either way: every nont_k = 6/6. The metric saturates at k=6, so it had no headroom todiscriminate. The re-run needs k ≥ 12 and should rank on mean_pairwise_overlap, which did vary (0.043–0.075).                                                               
So the honest position is unchanged from before the run: nothing is proven. G7, G8 and G9 remain off, and the programme's status stays built. What went live is measurement plus prompt context that can never cite, score or kill — which is why shipfe here.

Handoff written to checkpoints/LATEST.md with the two harness defects, thee four defects fixed and committed this session.

Safe point — type /clear (state saved, nothing will be so how do wee conclude and ship
