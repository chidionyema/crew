---
captured: 2026-08-08T23:22:30+00:00
session: 93fb799e-57b6-4eb3-bcbc-9f114d4fca18
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2184
source: founder prompt, verbatim (founder-doc-capture.py)
---

Rest of the pages

I've only actually looked at /ideas and / (homepage) — that's it. I have not checked /pack/[id], /pricing, /how-it-works, /kill-log, /about, /faq, /account, /orders, /sample. Nothing to report there yet — I'm not going to guess.

The jumping issue

Checked this directly rather than assume. There's already a committed, unmerged fix for it, sitting on this exact branch, one commit before my two:

7535a65 fix(storefront): filter-sheet scroll jump, header tap targets, brand-colour logo, compact-on-scroll

▎ "Filter jump: opening the filter sheet unmounted the inline StepFlow wizard to zero height above the reader's scroll position, shifting everything below it up while scrollY stayed fixed... A ResizeObserver-backed same-height spacer now stands in while the sheet is open."

But git log main..HEAD shows this branch is 2 commits ahead of main and not merged (git branch -vv confirms it's only pushed to origin/fix/storefront-header-logo-filter-jump, not landed). So unless your deploy pulls from this branch specifically, the live mumchimp.com you're looking at almost certainly does not have this fix yet — that's very likely why you're still seeing it jump. I haven't confirmed what mumchimp.com actually deploys from, so treat that last part as inference, not a verified fact.

My two new fixes today (CategoryGraph.tsx, AmbientKillColumn.tsx) are on this same unmerged branch, for the same reason — nothing I did today is live either.

So the real open question is deploy, not more code: does this branch need a PR + merge to go live, or is there a separate deploy step? Worth confirming before I sink more time into "rest of the pages" fixes that would also sit unshipped.

  Ran 1 shell command

Safe point — type /clear (state saved, nothing will be lost).

✻ Sautéed for 1m 17s
                                                                                                              2% until auto-compact
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯ the whole of desktop looks a shanble
──────────────────────────────────────────────────── ship and review
