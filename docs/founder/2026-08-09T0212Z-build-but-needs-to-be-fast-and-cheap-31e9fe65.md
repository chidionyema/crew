---
captured: 2026-08-09T02:12:29+00:00
session: 56c650af-b61b-420c-90d5-3abed29fae52
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2903
source: founder prompt, verbatim (founder-doc-capture.py)
---

build but needs to be fast and cheap to dp so 

hermes-agent 3d268f70de (25 files) — double-named destinations 39 → 0, via 81 label rewrites across 23 modules, derived from one canonical name per callback rather than hand-typed. Four survivors are declared, not drift (_DESIGNED): sdlc.py's four are stage names in a pipeline the panel prints and glosses on screen (sdlc.py:198-211), and two client-facing surfaces say Contact/Feedback.

The finding that mattered wasn't in the plan. After the scanner read 0, I rendered all 46 panels and read the keyboards. cockpit.render_run was emitting two buttons both labelled ♻️ Restart on one screen — one restarting the signal engine, one the Prospector scheduler. Each sits under its own Group heading, so the source reads as unambiguous; it isn't, because headings live in the message text and a Telegram inline keyboard is one flat grid. Two identical adjacent buttons, different daemons. The same blind spot hid 💵 Spend vs 💵 Spend cap (cockpit._TUNE_GROUPS is a 3-tuple; the scanner walks only 2-tuples) and 📦 Prospector colliding with 🔭 Prospector.

So both guards ship: test_destination_vocabulary.py asks whether a destination has one name; the new test_no_screen_says_one_word_twice.py renders the cockpit and asks whether a name has one destination. Its first version rendered nothing — every call raised, and it reported "0 conflicts" — so it now carries a floor of 40 rendered panels.

One deletion needed care: the zero-yield row's 🧪 Golden set named a destination that doesn't exist (it opened scheduler logs), but has_zero_yield is a real finding whose only expression was that row. The finding moved into the text; both destinations stay one tap away below.

Receipts, all re-run:
- tests/gateway/operator_shell/ → 671 passed, 5 skipped, 0 failed
- Live sweep → 46 panels rendered, 0 screens showing one word twice
- Mutation check → restoring the ♻️ Restart collision fails the new guard, offender named
- Gateway pid 70470 started 296s after the newest panel edit, so it serves this code
- The 24 test_restart_notification.py failures are pre-existing — identical on pristine HEAD via git stash; pytest.mark.asyncio isn't registered in this env

Doc committed as 314a7ea.

One R7 item is left and it's your call, not a fix. _UNBUILT quarantines 15 actions — fix_all, fix_all_safe, onboard, score, dependencies, correlate, compliance, logs, estate_health, operator_mode, setup_wizard, rsi_run, rsi_pause, rsi_resume, deploy — emitted from 30 literal button sites (re-derived on disk; my earlier "~35" was wrong). Tapping any renders ⚠️ Unknown action. The ratchet stops new dead buttons appearing, but those 30 still do nothing. Each needs build-or-delete from you.

R4 (node priority per pipeline step) and R5 (extreme visibility) remain untouched; prerequisites are in the handoff.

Safe point — type /clear (state saved, nothing will be lost
