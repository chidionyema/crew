---
captured: 2026-08-16T13:10:04+00:00
session: 1154e812-5062-47e4-857b-717235dd294e
cwd: /Users/chidionyema/Documents/code/prospector
chars: 1764
source: founder prompt, verbatim (founder-doc-capture.py)
---

DONE: your directive is now a written principle, and it changes what "finished" means for everything still to build.

P10 — if it can change, the operator changes it from the console, not from source. Committed as d638d03. The test it sets: if this value were wrong at 9pm on a Sunday, who fixes it and how? If the answer involves an editor, a build, a deploy or an agent, it is in the wrong place. That is stronger than the rule we had — a YAML file only a developer can edit was already allowed, and it is not ops-driven either.

It also applies to actions, not just values: a console that reports 38 stranded packs and cannot start the repair is a report, not a control. Every automation with a safe repair gets a button.

You caught me breaking it in the same minute I wrote it. I put ByteSync's company number and address into config.ts as TypeScript constants — right data, wrong place, because changing it needs a build and a deploy. That edit is on disk, uncommitted, and its real home is operator-editable state with a console screen. I have written that as the next step rather than leaving it looking done.

---
Also recorded: BYTESYNC LTD, 17182157, Flat 26, 26 Golders Green Crescent, London NW11 8LE — confirmed against Companies House, and the number matters because five dissolved companies share the name. VAT stays empty and every surface branches on it, so nothing invents one.

Still yours to run, because the classifier blocked me:
! gh repo edit --visibility private --accept-visibility-change-consequences

Handoff written to checkpoints/2026-08-16-launch-ops-handoff.md — a dated file, because a concurrent session owns LATEST.md and overwrote an earlier handoff of mine today.

Safe point — type /clear (state saved, nothing will be lost).
