---
captured: 2026-08-17T10:24:54+00:00
session: 1154e812-5062-47e4-857b-717235dd294e
cwd: /Users/chidionyema/Documents/code/prospector
chars: 1713
source: founder prompt, verbatim (founder-doc-capture.py)
---

---
The measured table

STOP EVENTS: 695   TOOL CALLS: 41319   RATE: 1.68 per 100

  54   7.8%  Agent                 <- subagent launches
  42   6.0%  Bash:python -m pytest <- test runs
  34   4.9%  AskUserQuestion       <- asking instead of deciding
  30   4.3%  Bash:for i            <- shell loops
  20   2.9%  Bash:git add
  32   4.6%  python heredocs (two buckets)
   8   1.2%  Bash:git commit

  month    stops   calls   rate
  2026-07     25    3947   0.63
  2026-08    670   37375   1.79

Why this beats writing another memory file

The 333 memory files are prose an agent may or may not read. These 695 events are you, at a specific point, saying "not that" — already attached to the trajectory that earned it. It was on disk the whole time and nothing had ever opened it.

The finding I would have got wrong from memory alone: I was about to build a delegation guard for read-only drift. The data says drift causes 1% of stops. I would have shipped a rule that fixed almost nothing and felt productive.

The one rule this data justifies

Not a guard on reading. A guard on launching: before an Agent, a full pytest, or a loop over many items, the cheap check first — does the answer already exist on disk. That is three of your top four buckets, 18% of every stop.

The fourth is AskUserQuestion, and the rule there is: decide, state the assumption, keep moving.

Wiring

reflect.py --trend is the scoreboard. Run it after any behaviour rule lands; if August's 1.79 doesn't fall, the rule didn't work. I'd put it on the same monthly cadence as the cost audit rather than a hook — it takes ~2 minutes over 1.2 GB and nothing needs it per-session.
 sane issue, we need to prve this works and is effective
