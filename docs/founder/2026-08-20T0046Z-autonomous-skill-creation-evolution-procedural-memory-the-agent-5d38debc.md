---
captured: 2026-08-20T00:46:24+00:00
session: 43c6022c-6d9f-4253-b9ba-742a5830fbbc
cwd: /Users/chidionyema/Documents/code/prospector
chars: 8810
source: founder prompt, verbatim (founder-doc-capture.py)
---

Autonomous Skill Creation & Evolution (Procedural Memory)The agent monitors its own workflow patterns, automatically writes new SKILL.md modules when it detects repetitive tasks, and refines those skills based on execution outcomes.  2. Deterministic Logit-Masking & Grammars (Outlines)Enforces strict JSON schemas or Context-Free Grammars directly at the token sampling layer. Guarantees 100% syntactically valid tool calls and eliminates JSON parsing errors.3. Isolated Subagent OrchestrationSpawns parallel, ephemeral worker subagents to execute long-running or context-heavy sub-tasks, returning only final results to keep the parent agent's context clean.  4. Three-Layer Hierarchical Memory (Working, Episodic, Semantic)Combines short-term chat context, timestamped logs of past actions, and graph-backed project knowledge. Allows context to survive /clear commands across weeks of work.  5. Dynamic Model Context Protocol (MCP) Server MountingDiscovers and mounts external tool servers (Postgres, Linear, GitHub, Figma) over standard MCP interfaces on demand, loading tools only when required.  6. Self-Healing TDD (Test-Driven Development)Writes failing unit tests, executes them, intercepts error stack traces, and iteratively patches code in an automated loop until all tests pass.7. Visual Regression & Headless Browser Drive (browser-use / Playwright)Drives browser engines to interact with live web applications, take DOM snapshots, and perform pixel-level screenshot comparisons across PR builds.8. Sandboxed Ephemeral Execution (Modal / Daytona / E2B)Offloads raw terminal commands, scripts, and build environments into isolated micro-VMs, protecting host machines from unsafe execution.9. Trajectory Pruning & Log CompressionFilters out repetitive tool outputs, minifies stack traces, and prunes historical CLI logs before feeding context back to the model—eliminating 5+ minute compaction stalls.10. Human-in-the-Loop Security & Approval GatesMonitors shell execution for destructive actions (e.g., rm -rf, database drops, production deploys) and pauses execution for human approval before proceeding.1. Frontend Design (frontend-design)What it does: Provides design philosophies, spacing systems, and custom typography choices before generating frontend code.  Why it matters: Breaks the "statistical center" of bland AI-generated UI (e.g., standard purple-on-white cards) to output polished, production-ready interfaces.  2. Code Reviewer & Simplifier (simplify)What it does: Runs an immediate refactoring pass over any code the agent writes or modifies.  Why it matters: Flags single-responsibility violations, dead code, redundant logic, and performance bottlenecks before presenting the final code to you.  3. Browser Automation (browser-use / playwright)What it does: Connects the agent to a headless browser instance to navigate URLs, click DOM elements, take screenshots, and test deployed apps.  Why it matters: Expands the agent beyond text generation into an end-to-end QA engineer and web scraper.  4. Test-Driven Development (tdd / superpowers)What it does: Enforces a strict red-green-refactor loop by requiring the agent to write failing unit/integration tests before writing implementation code.  Why it matters: Eliminates regressions and forces the agent to treat tests as hard specifications rather than afterthoughts.  5. Model Context Protocol Builder (mcp-builder)What it does: Teaches the agent how to construct Model Context Protocol (MCP) servers in Python or TypeScript.  Why it matters: Allows the agent to dynamically build its own API integrations and tool bindings on demand.  6. Structured Generation (outlines / pydantic-ai)What it does: Enforces schema validation (Pydantic models, JSON schemas, or grammars) on agent outputs.  Why it matters: Ensures function calls and agent-to-agent communication never break due to malformed JSON or invalid syntax.7. Persistent Memory Graph (cognee / honcho)What it does: Connects the agent to a self-hosted knowledge graph or persistent memory provider.  Why it matters: Preserves context, architectural decisions, and project conventions across session wipes and CLI restarts.8. Software Architecture & Clean Code (architecture)What it does: Inject design guidelines (Clean Architecture, SOLID, Domain-Driven Design) into the prompt context.  Why it matters: Prevents agents from generating monolithic, tightly coupled code files in large repos.  9. Security & Dependency Auditing (shannon / pip-audit)What it does: Scans codebase changes for security flaws (hardcoded credentials, injection risks) and checks dependencies for known CVEs.Why it matters: Prevents supply-chain vulnerabilities and insecure code patterns from reaching production.10. Task Execution & Planning (execplan / maestro)What it does: Manages long-running, multi-step tasks using structured plan files and step tracking.  Why it matters: Keeps autonomous agents from getting stuck in execution loops or losing track of high-level project goals. 1. Agent-to-Agent Commerce (internet-court-skill)What it does: Acts as a trust and settlement layer that allows your local CLI agent to negotiate, pay, and securely delegate tasks to other external agents using x402 payments and ERC-7710 permissions.  Why it matters: Moves agents beyond isolated scripts into an interoperable machine economy where they can hire each other for specialized micro-tasks.2. Cross-Instance Swarm Memory (icarus-plugin)What it does: Syncs high-value architectural decisions, bugs, and execution outcomes across multiple different CLI instances or team members via shared state files.  Why it matters: If Agent A solves a tricky dependency bug on your laptop, Agent B on your coworker's machine instantly knows how to fix it without starting from scratch.3. Native Infrastructure Diagramming (drawio-skill)What it does: Bypasses basic ASCII or Markdown and allows the agent to directly generate natively editable .drawio architecture diagrams using specialized presets.  Why it matters: Your agent can update your cloud architecture visual documentation in the exact same pull request where it updates the Terraform code.4. Literate Programming Engine (litprog-skill)What it does: Forces the agent to weave prose, system diagrams, and source code into single .lit.md files before compiling them down to raw, executable code.  Why it matters: Radically improves the maintainability of AI-generated code by forcing the LLM to explain its logic mathematically and structurally before the code executes.5. Meta-Skill Evolution (SkillClaw)What it does: An autonomous "janitor" skill that continuously scans your agent's skills directory, deduplicating redundant instructions and rewriting other skills to be more token-efficient.Why it matters: Prevents context bloat and prompt drift as your agent accumulates hundreds of custom workflows and tasks over months of use.6. Framework-Mapped Infosec (Anthropic-Cybersecurity-Skills)What it does: Injects over 800 highly structured security checks mapped directly to enterprise standards like NIST CSF 2.0 and MITRE ATT&CK.  Why it matters: Upgrades the agent from a generic coder to a compliance-grade security auditor that enforces strict enterprise security postures on every code change.7. Self-Hosted Knowledge Graphs (cognee)What it does: Hooks the agent into a local graph database, transforming unstructured chat history into a queryable semantic web of entities (e.g., "User -> prefers -> functional programming").Why it matters: Solves the "needle in a haystack" problem of vector databases by giving the agent deterministic, relational recall of decisions made weeks ago.8. Web3 Smart Contract & Oracle Integration (chainlink-agent-skills)What it does: Gives the agent the ability to read on-chain data, interact with smart contracts, and fetch real-world oracle data natively.Why it matters: Essential for crypto-native development; your agent can read live token states or deploy testnet contracts without you having to write Python wrapper scripts for it.9. Zero-Shot Media Pipeline (black-forest-labs/skills)What it does: Directly integrates FLUX image and video generation capabilities with specialized prompting logic.  Why it matters: A frontend agent can now mock up a UI layout, write the React code, and generate the missing placeholder illustrations and video assets in a single CLI command.10. AI-Artifact Scrubbing (avoid-ai-writing)What it does: Runs a strict two-pass audit on all agent-generated text (documentation, PR descriptions, emails) against a 112-entry replacement table to strip out standard LLM vocabulary ("delve", "robust", "testament").  Why it matters: Makes agent-authored communication completely indistinguishable from human writing, preventing "AI fatigue" in team environments.Whic also
