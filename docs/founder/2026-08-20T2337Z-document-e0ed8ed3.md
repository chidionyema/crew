---
captured: 2026-08-20T23:37:58+00:00
session: 74f4ed5c-57bc-4844-abc2-5dc3d7892de6
cwd: /Users/chidionyema/Documents/code/prospector
chars: 2362
source: founder prompt, verbatim (founder-doc-capture.py)
---

.)
  -> ~/.claude/scripts/rule-guard.py:105   subprocess.run(("git", *args), ...)
  -> resolves through the shim again (child inherits PATH with shim first)
  -> core.py:49 ...

core.py:32 _real() de-recurses only the shim's own os.execv at :83. It never sanitises PATH for the guard child. Two samples 5s apart: rule-guard 212→222, core.py 204→221; 193% CPU, 2842 MB RSS; total processes 1101→1126 in a minute.

I tried mv policy/bin policy/bin.DISABLED — the classifier denied it, correctly, because it is another session's directory. I did not route around it. I sent the diagnosis with the file:line to all four busy sessions and did not ask any of them to run my denied command.

What I need from you: run this yourself, or tell me to. It is one reversible command.

mv /private/tmp/claude-501/-private-tmp-claude-501--Users-chidionyema-Documents-code-prospector-3fa47c70-c6d2-4273-9620-19dc9810b132-scratchpad-wt-storeroot/0d5d261b-d4bc-4fc1-a680-9c2b5af6a13c/scratchpad/policy/bin \
   /private/tmp/claude-501/-private-tmp-claude-501--Users-chidionyema-Documents-code-prospector-3fa47c70-c6d2-4273-9620-19dc9810b132-scratchpad-wt-storeroot/0d5d261b-d4bc-4fc1-a680-9c2b5af6a13c/scratchpad/policy/bin.OFF

PATH then stops resolving there and git falls through to /usr/bin/git, so no session loses work — they just run unguarded until the shim is fixed.

Risks of the move you proposed, if you still want it later

1. 62 worktrees break instantly. Each holds an absolute path: wt-offsite/.git contains gitdir: /Users/chidionyema/Documents/code/prospector/.git/worktrees/wt-offsite. Five sessions are live in them now.
2. 11 LaunchAgents name the path, including PROSPECTOR_STORE_DIR = …/prospector/store. The canonical store moves with it.
3. ~/.claude holds 8 references — 4 lines in settings.json, 5 hook scripts. settings.json is read once at process start, so the five live sessions would keep the dead path until they restart.
4. The signing key is in there and is untracked: .lux/keys/agent.pem, 64 bytes, mode 600. Lose it and every commit gate in the estate blocks.
5. Not atomic. Any session mid-commit holds .git/index.lock.

One thing that IS genuinely in iCloud and shouldn't be: 123 entries at the root of iCloud Drive, and they are unpacked npm packages — code-frame/, compat-data/, helper-plugin-utils/, dist/, cjs/, generato how do we address
