---
captured: 2026-08-21T18:53:14+00:00
session: 16863be1-f969-4762-97e5-c29ad2f56313
cwd: /Users/chidionyema/.claude
chars: 2404
source: founder prompt, verbatim (founder-doc-capture.py)
---

The map confirms what you felt: your local setup built a 20,000-line self-policing bureaucracy that spends more compute time running internal checks than doing actual work.Having 58 governance scripts, 37 active hooks, and a single script (rule-guard.py) making 106 synchronous git calls per tool execution is a recipe for index lock collisions, timeouts, and constant agent panic loops.Summary of Estate Map & Triage StrategyMetricDiagnosisImmediate Action15 Dead Scripts (3,340 lines)Pure unreferenced dead weight.Delete immediately. No replacement needed.rule-guard.py (1,256 lines)106 git calls per tool invocation. Primary lock hog.Disable in settings.json. Replace with static rules.guard-autocommit.pyFails on repository timeouts; leaves orphaned .git/index.lock.Disable or decouple from pre-tool hooks.37 Firing Hook EntriesIntercepts every single agent thought and command.Prune to $\le$ 3 critical events (e.g., standard error logging).Step-by-Step Execution PlanStep 1: Delete the 15 Orphaned ScriptsSince the multi-angle check verified these 15 files are completely unreachable by launchd, hooks, or imports, run your deletion command to instantly drop 3,340 lines of dead code:Bash# Execute your local script or manual rm on the 15 unreferenced files in scratchpad/estate-map.json
Step 2: Disarm rule-guard.py in settings.jsonComment out or remove rule-guard.py from your Claude/Agent hook config. Running 106 git operations in a blocking pre-tool hook means any sub-second command gets bogged down in 10-20 seconds of filesystem overhead.Step 3: Stop Synchronous Git Auto-CommitsRemove guard-autocommit.py from active execution hooks. Auto-committing state inside active agent loops guarantees file lock contention. If you need git tracking:Run commits asynchronously on a background timer outside the agent process, orCommit manually at logical session milestones.Step 4: Replace Dynamic Python Enforcement with Static CLAUDE.md / AGENTS.mdDelete the dynamic Python "law enforcement" scripts (goal-guard.py, LAW 9 hooks) and write 10-15 explicit, high-priority bullet points directly in your project's CLAUDE.md:Example: "Maximum 2 tool retries on failure. Never run git commands inside pre-tool hooks. Stop and ask if stuck."Disarming rule-guard.py and deleting those 15 dead files today will instantly eliminate the lock contention and give you a fast, stable terminal environment again.
