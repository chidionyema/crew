---
captured: 2026-08-28T20:15:36+00:00
session: f3f21d6e-8df9-44b8-ae46-def299e0298c
cwd: /Users/chidionyema/dev/code
chars: 2089
source: founder prompt, verbatim (founder-doc-capture.py)
---

and also You are exactly right. Approving ADR 0002 only stops the bleeding. If we leave a graveyard of old, badly formatted markdown files, the TechDocs portal will still be a mess.And you've hit on a classic loophole: when you ask an agent to do "research" or "automate" something, they often treat it as a one-off script output rather than "documentation as code." They dump the results wherever is easiest because there is no physical wall stopping them.To fix the past and future-proof the agents in one shot, we have to turn the Diátaxis structure into a strict file-system constraint. Here is the exact plan to do both.1. The Retroactive Sweep (Fixing the Past)We cannot manually sort dozens of old markdown files. We will script the migration:The Audit Script: We run a quick bash script to find every .md file in the repository.The Filter: We filter out the approved paths (docs/tutorials/, docs/how-to/, docs/reference/, docs/explanation/, docs/decisions/) and the root governance files (like README.md, FOUNDER.md).The Migration PR: Everything left over is the "rogue documentation" list. We generate a single PR that maps and moves these orphan docs into their correct Diátaxis folders, updating the mkdocs.yml navigation tree simultaneously.2. The Agent Guardrail (Future-Proofing)To stop agents from ignoring the docs process during research or automation tasks, we have to take away their ability to write non-compliant files.The docs-guard Hook: We add a strict rule to the Write and Edit tools. If an agent tries to save a .md file outside of the exact docs/ folder structure (or authorized root files), the system refuses the write.Enforcing Research: When you ask an agent to "research," they will hit this wall if they try to dump it in a random temp file. The guardrail forces them to categorize their research properly—usually into docs/explanation/ (for concepts) or docs/reference/ (for data)—before the system accepts their work.This completely removes the "honor system" for documentation. If it isn't in the Diátaxis structure, the code literally will not commit.
