---
captured: 2026-08-29T00:02:01+00:00
session: a0d64ea4-d03d-4d1f-84e1-e1739e05c615
cwd: /Users/chidionyema/dev/code/idp/.claude/worktrees/crew459-portal-polish
chars: 2133
source: founder prompt, verbatim (founder-doc-capture.py)
---

1. Shift Left: Never push a lint error againA lint warning should fail in 400 milliseconds on your laptop, not 13 minutes later in CI.The Fix: Implement pre-commit hooks. Force linting, formatting, and fast unit tests to run locally before Git even allows the commit to be created.The Result: You literally cannot push the kind of typo that cost us a cycle tonight.2. Split the CI Pipeline: Fail Fast, Pass SlowRight now, you are paying the 13-minute "infrastructure tax" for every tiny code change.The Fix: Split CI into two distinct, sequential stages.Stage 1 (The Gate): Linting, unit tests, static analysis. This takes 30 seconds.Stage 2 (The Heavy Lift): Build 4 containers, boot k3s, deploy.The Result: If a test breaks, CI fails and notifies you in 30 seconds. You only pay the 13-minute k3s tax when the code is actually fundamentally sound.3. Attack the 13-Minute BottleneckIf we still need 13 minutes when the code is good, we need to optimize that heavy lift.Aggressive Caching: Are you using BuildKit remote layer caching for those 4 containers? If you only touch a Python script, the heavy apt-get install layers should instantly pull from cache.Parallelization: The container builds and the k3s cluster boot should be happening simultaneously, not sequentially. Boot the cluster while the images are compiling.Smart Matrix: If you only modify code for Container A, the CI should be smart enough not to rebuild Containers B, C, and D.4. The Radical Step: Ephemeral Dev EnvironmentsIf you want to move at the speed of thought, building containers and booting clusters on every push is the wrong development paradigm entirely.The Fix: Use a tool like Tilt or Skaffold.How it works: You keep one throwaway k3s cluster running in the background. As you hit "save" on your laptop, Tilt hot-swaps the code directly into the running pods in milliseconds, bypassing the container build and cluster boot entirely.The Result: You get the safety of testing in real Kubernetes, but the feedback loop is 2 seconds instead of 13 minutes. CI is relegated to being the final safety check before merging, not your primary debugging loop.
