---
captured: 2026-08-30T02:33:42+00:00
session: 41fd24d8-96a0-417e-bc3f-b7b15aceb026
cwd: /Users/chidionyema/dev/code/crew
chars: 1911
source: founder prompt, verbatim (founder-doc-capture.py)
---

xactly. That Git tag change is the exact bottleneck we are eliminating in crew#692 CP1.Right now, your CI builds the image and securely pushes it to the GitHub registry, but the cluster (via Flux) just sits there because the Kustomize overlay in your repo still points to the old image tag.To make this a fully seamless, hands-free deployment, we have to close that gap. Here is how you wire it up for true GitOps automation:Closing the Gap (crew#692 CP1)You have two elite ways to automate this tag change so Flux instantly triggers the RollingUpdate:1. The CI Commit (The Direct Route)This is the most common and immediate fix. You add a step at the very end of your GitHub Actions build pipeline.The Action: After the image pushes to GHCR, the pipeline runs kustomize edit set image storefront=ghcr.io/your-org/storefront:${GITHUB_SHA}.The Commit: A bot token commits that single manifest change and pushes it directly back to the repo.The Result: Flux detects the Git commit, reconciles the cluster state, and starts scheduling the new pod.2. Flux Image Update Automation (The GitOps "God Mode")Instead of giving your CI pipeline write-access to your repository, you let the cluster do the work.The Action: You configure Flux's ImageRepository and ImageUpdateAutomation controllers.The Scan: The cluster actively watches your GitHub Container Registry. When it sees a new semantic version or branch tag arrive...The Result: Flux itself creates the Git commit updating your manifest, pushes it to the repo, and deploys it automatically.Once this tag automation is in place, your release is invisible. You merge the code, the tag updates, the new pod spins up, it passes the /api/health check, and the old pod is gracefully drained (with that 5-second preStop sleep you are adding in CP3 to protect in-flight traffic).Which path are we taking for CP1 to close the loop—the CI commit or Flux native automation?
