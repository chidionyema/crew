---
captured: 2026-08-30T23:04:26+00:00
session: a7b41022-3074-43c7-bb13-a1d7e07adff1
cwd: /Users/chidionyema/dev/code/.wt-otto-token
chars: 2696
source: founder prompt, verbatim (founder-doc-capture.py)
---

To permanently eradicate this entire class of bugs—blind text substitution corrupting Kubernetes CRD strict-typing rules—you have to change how configuration flows from source to cluster.The root pathology here is architectural: using a text-stream replacement tool (flux envsubst) on a structural data format (YAML) bound by strict Kubernetes CRD schemas. Text substitution doesn't know the difference between a string ID ("4740261") and a numeric value (4740261). When Kustomize unquotes or processes the placeholder and Flux injects a raw number, the YAML parser implicitly casts it, and the Kubernetes API server rightfully rejects it.Here is the elite, exponential blueprint to eliminate this defect class forever across your enterprise estate:Phase 1: Kill Blind Text Substitution for Typed CRDsNever use flux envsubst (or raw text envsubst) on Custom Resources where fields require strict typing (like IDs, versions, or hashes that happen to be all-numeric).The Replacement: Transition from text substitution to Kustomize Native Replacements or Helm Values Injection.Why it works: Kustomize replacements and Helm engines operate on structured abstract syntax trees (AST) rather than raw text streams. They preserve the target field's node type (string vs int), ensuring quotes and types survive the pipeline intact.Phase 2: Shift-Left Schema Conformance (kubeconform)Right now, you are finding out about type mismatches during a live cluster dry-run or API server admission check. That is too late in the feedback loop.The Fix: Insert kubeconform into your GitHub Actions CI pipeline.How it works: Before a PR can merge, kubeconform takes your fully rendered, post-processed manifests and validates them directly against the official, exact CRD schemas (including External Secrets, Kyverno, and Flux) fetched dynamically from your cluster version.The Result: A PR attempting to pass an integer into a string field fails instantly in CI with a precise schema mismatch error. It becomes physically impossible to merge a type violation.Phase 3: Structural Guardrails in the Generator TemplatesIf you must use environment injection for specific templates, wrap them in structural validation rules or enforce string casting at the generator level using yq node-type operators rather than raw substitution strings. For instance, enforcing strenv() instead of loose interpolation guarantees the output node is explicitly flagged as a string (!!str) before hitting the YAML emitter.Summary: You stop chasing unquoted integers by removing text-stream substitution from typed resource pipelines entirely and replacing it with AST-aware Kustomize/Helm handling backed by a strict kubeconform CI gate.
