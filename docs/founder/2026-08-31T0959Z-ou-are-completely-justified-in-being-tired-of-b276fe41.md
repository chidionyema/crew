---
captured: 2026-08-31T09:59:00+00:00
session: a7b41022-3074-43c7-bb13-a1d7e07adff1
cwd: /Users/chidionyema/dev/code/.wt-crew-crewai
chars: 2629
source: founder prompt, verbatim (founder-doc-capture.py)
---

ou are completely justified in being tired of this. You are being pinged on your physical phone with a FOUNDER ACTION push notification just to approve turning on a search box in Backstage. That is the definition of amateur hour.The agent correctly diagnosed its own flaw—"a law without a protocol is a wish"—but its proposed solution (dod-guard.py) is just a chat filter trying to police the agent's own text output. That isn't a protocol; that's a band-aid on a broken workflow. Furthermore, treating a read-only internal tooling config (Backstage search) as a blocker requiring CEO-level cryptographic approval is a massive misallocation of your time.Here is the elite, scorched-earth blueprint to fix this documentation gap structurally and stop the agent from treating you like a micro-manager.1. The "No Docs, No Merge" CI Gate (True Protocol)You do not enforce documentation by scanning an agent's chat reply. You enforce it at the Git layer.The Fix: We drop a structural check into your GitHub Actions PR pipeline.How it Works: If a Pull Request modifies application code (src/, Store.Api/, etc.) but the git diff shows zero additions to the docs/ directory, the CI pipeline immediately fails with a fatal error: "Missing Architectural Record."The Result: The agent literally cannot merge code without writing the documentation first. You never have to ask where the doc is, and the agent never has to run a python script to remind itself to tell you.2. Redefine "FOUNDER ACTION" (Blast Radius Matrix)The agent's operating model is fundamentally broken if it requires your permission for Backstage plugins.The Fix: You must explicitly define the agent's autonomous blast radius in its system prompt/rules.The Rule: FOUNDER ACTION and physical phone pings are strictly reserved for:Infrastructure state mutation (destroying/creating nodes).RBAC or Security policy changes (like the kube-system lock).Production database migrations.The Execution: Internal tooling configurations (Backstage, testing scripts, guardrails) are designated as Auto-Merge. The agent writes the config, CI validates it, and it ships to the cluster without pausing for your approval.3. Auto-Execute the Backstage ConfigDo not reply APPROVE search. That validates the agent's bad behavior. The Backstage search plugin is a stateless, read-only indexing tool.The Fix: Order the agent to immediately merge the Backstage search PR under the new "Internal Tooling Auto-Merge" rule, and implement the Git CI Docs gate.Stop managing the agent's chat replies and start managing the repository's physics. If the repo demands docs, the agent will write docs.
